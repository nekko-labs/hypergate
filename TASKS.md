---
status: active
created: 2026-06-28
owner: Philip
type: code
---

# Execution Plan — Hypergate

> Converted from executionplan.md on 2026-06-29. ✅ = done per the prior plan; Part 1 below is the technical plan, Part 2 is the task checklist.

> **The plan + the build log, in one file.** The top half (Part 1) is the technical plan — how we build what `SPEC.md` describes. The bottom half (Part 2) is the task list — Now / Backlog / Shipped, recording how past features were built and how future ones will be. (Merged from the former `plan.md` + `tasks.md`.)

---

# Part 1 — Plan (how we build it)

> **How.** Stack, architecture, conventions, and the execution backlog. The *what & why* lives in [`SPEC.md`](SPEC.md).

## 1. Stack (decided — do not relitigate)

| Area | Choice | Why |
| --- | --- | --- |
| Language / repo | **TypeScript, npm workspaces** (Node ≥20; runnable with **bun**) | Matches the org stack (Nekko Notes, Kotrain) so `@hypergate/ui` can be shared into Kotrain; no pnpm/yarn (duplicate-React breakage). |
| Daemon | Node/Bun long-running process (`hypergated`) — HTTP management API + MCP gateway + supervisor | A ToolHive rival needs a resident runtime to launch/supervise servers and hold the gateway. |
| MCP | `@modelcontextprotocol/sdk` (client per managed server, one aggregated server for the gateway) | Official SDK; same dep Kotrain + the Notes CLI already use. |
| UI | Vite + React + TS + Tailwind + Zustand (Nekko design tokens) | Consistent with Notes/Kotrain; the UI package embeds as a Kotrain pane. |
| Isolation | **RuntimeAdapter** interface: `ProcessRuntime` (default) + `DockerRuntime` (opt-in), user-selectable at setup | Don't force Docker; make the security tradeoff explicit (see spec §1). |
| Store | **SQLite (`node:sqlite`, WAL)** at `~/.hypergate/hypergate.db` | Durable, queryable usage history and logs. Replaced whole-file JSON rewrites, which capped history at 2000 calls. Built into Node ≥22, so no native addon to sign or notarize. |
| Desktop shell | **Rust, per-user logon agent** (`apps/shell`): tray icon + menu + autostart + CLI + sandbox launcher | The hard parts are OS-level (tray, login items, Job Objects, keychain), not UI. Rust gets `windows-rs`/`objc2`/`zbus` as plain dependencies. Replaces the Electron plan and the PowerShell tray. |
| Desktop UI | **The existing web UI, in a native window** (wry / the OS webview; browser fallback) | One UI, two frames. Amended 2026-07-28 (was browser-only): user feedback wanted a real desktop app, and wry renders the daemon-served UI without bundling an engine. See Epic 11. |
| Test/CI | Vitest (core units) + a daemon smoke/probe (spawn a stdio server through the gateway) + GitHub Actions | Same gate discipline as Notes. |

### 1.1 Stack amendment (2026-07-27)

Superseded the Electron row above. Decisions and the reasoning, so this isn't relitigated either:

- **Per-user logon agent, never a Windows Service / `launchd` daemon at root.** Managed MCP servers need the user's `PATH`, home dir, npx cache, per-user Docker socket, and the OAuth grants under `~/.hypergate/`. A SYSTEM/root service gets none of that, adds UAC friction, and Windows Session 0 isolation would kill any UI.
- **Rust shell, TypeScript daemon.** The shell owns what needs native APIs (tray, menu, login items, keychain, resource limits). The daemon keeps the MCP protocol work in TypeScript, where the official SDK lives and moves fastest. The daemon stays independently runnable so headless Linux, WSL, and containers work with no tray at all.
- **Rejected: Flutter and React Native.** Both are UI toolkits, not service runtimes; neither can be a background agent without keeping a rendering engine resident. React Native additionally has no Linux desktop target.
- **Rejected: billing, accounts, and a hosted control plane.** Local-first with no account is the differentiator against ToolHive, and SPEC §1 sells it directly ("no account or cloud required", "the daemon makes no outbound calls on its own"). We are not trading that for a revenue line. Usage metering exists purely so the *user* can see their own traffic; it never leaves the machine.
- **Tray crates, not full Tauri.** `tray-icon` + `muda` + `tao` are the same crates Tauri uses for its tray, so tray behavior is identical. Originally we linked no webview ("we never render one"); **amended 2026-07-28**: user feedback asked for a real desktop app, so the shell now also links `wry` (Tauri's webview crate) and renders the manager UI in a native window. This is exactly the extension path this decision reserved. The cost accepted with eyes open: on Linux `webkit2gtk-4.1` joins GTK as a hard load-time dependency of the one binary (declared by the .deb/.rpm; mainstream desktops ship it; headless boxes run `hypergated` alone). Still no Tauri config/IPC layer; the window is ~90 lines.

## 2. Repo Layout

```
hypergate/                         GitHub: nekko-labs/hypergate (public, MIT)
  packages/
    shared/   types + API/IPC contract (ManagedServer, RuntimeKind, ServerStatus,
              RegistryEntry, GatewayConfig, daemon API request/response types)
    core/     pure-ish engine: registry catalog, server-config validation,
              RuntimeAdapter interface + ProcessRuntime + DockerRuntime, Supervisor,
              gateway aggregation logic  (+ *.test.ts)
    ui/       @hypergate/ui — shared React UI (embeds in Kotrain)
  apps/
    daemon/   hypergated — HTTP management API + MCP gateway (stdio + HTTP/SSE) + supervisor host
    web/      Vite React app (standalone UI) → talks to the daemon
    shell/    Rust: `hypergate` binary = tray agent + CLI + sandbox-exec launcher
  docker-compose.yml · tsconfig.base.json
```

## 3. Architecture

- **Daemon (`hypergated`)** holds the `Supervisor` (a map of `ManagedServer` → running instance via a `RuntimeAdapter`). One localhost port (7777) exposes:
  - **Management API** (HTTP, localhost): `GET /api/servers`, `POST /api/servers` (add), `POST /api/servers/:id/start|stop|restart`, `DELETE /api/servers/:id`, `GET /api/servers/:id/logs`, `GET /api/registry`, `GET /api/gateway` (URL + token + HTTP/stdio client snippets + uiUrl).
  - **Gateway**: an aggregating MCP server. For each running managed server it holds an MCP **client**; it merges their `tools` (namespaced `server__tool`; resources/prompts planned) and routes calls to the owner. Exposed over **stdio** (`hypergated --stdio`) and **streamable HTTP** at `/mcp`: stateless `StreamableHTTPServerTransport` (`sessionIdGenerator: undefined`, `enableJsonResponse: true`, a fresh transport+gateway per POST) so hand-rolled clients can speak plain JSON POSTs; bearer-token auth (token generated once into `~/.hypergate/gateway-token`, checked with `timingSafeEqual`; `HYPERGATE_NO_AUTH=1` escape hatch).
  - **Web UI**: the built `apps/web` served at `/` (path-sanitized static handler, SPA fallback), so one URL does UI + API + gateway.
- **RuntimeAdapter** interface: `start(server): Promise<RunningServer>`, `stop`, `logs`, `health`. `ProcessRuntime` spawns a child (allow-listed env, injected secrets, restricted CWD, resource limits, no shell). `DockerRuntime` runs a container (image, caps, network, limits). Chosen by `server.runtime` (default from setup).
- **UI** (`@hypergate/ui`) is transport-light: it calls the daemon's HTTP API. The standalone `apps/web` and the Kotrain tab both render it; only the base URL differs.

## 4. Conventions
- Pure logic in `packages/core` (unit-tested); the daemon wires IO. No secret ever logged. Local-first: no network egress from the daemon itself except what a managed server does.
- Build → verify → push to `main` (CI green). Conventional commits. Mock/test the supervisor with a trivial in-repo echo MCP server so tests need no external binaries.
- Every shipped feature → status + release in `SPEC.md`; new work broken down here first.

### 4.1 Versioning: every PR bumps the minor

**Every PR bumps the release version by one minor** (`0.17.0` → `0.18.0`), unless the PR itself says otherwise. Run it, don't hand-edit:

```
npm run version:bump          # minor, the rule
npm run version:bump -- --patch   # only when explicitly called for (a fix on a cut release)
npm run version:check         # assert every file still agrees
```

Why a minor every time: the publish job checks the tag against the root `package.json`, so a merged PR that left the version alone can never be released on its own. Bumping in the PR means `main` is always taggable, and the version in a bug report names the exact merge.

**Merging the bump is what cuts the release.** `version-release.yml` watches pushes to main that touch `package.json` and, when that version has no tag yet, tags it and dispatches `release.yml`; hand-tagging still works and is the fallback. It tags *and* dispatches because a tag pushed with `GITHUB_TOKEN` cannot trigger another workflow, so tagging alone would leave `release.yml` unfired. Before tagging it runs `version:check` and requires `docs/releases/<version>.md` to exist, so a partial bump or missing notes fails while there is still nothing tagged. Which means: **the notes file belongs in the same PR as the bump.**

The version is written in **seven** files that must move together, which is the whole reason for the script: root `package.json`, `apps/daemon/package.json`, the `VERSION` constant in `apps/daemon/src/index.ts` (served on `/health` and compared against the update feed), `apps/shell/Cargo.toml` + `Cargo.lock`, `.claude-plugin/marketplace.json`, and `plugins/hypergate/.claude-plugin/plugin.json`. `npm run version:check` fails on drift.

New features land under that version in `SPEC.md` (the **Release** column) and as a shipped entry here. A PR that ships nothing user-visible (docs, tests, CI) still bumps, so "unless mentioned" is the escape hatch, not a judgement call to make silently.

## 5. Execution Plan (backlog)

The epic-by-epic backlog lives in **Part 2 below** (single source of truth for task status; this section used to mirror it and drifted). Epics: 0 Scaffold · 1 Runtime & supervisor · 2 Gateway · 3 Registry & API · 4 UI · 5 Kotrain integration · 6 Distribution.

## 6. How We Work
Same as the org: spec is the product source of truth, plan is broken-down-first, build+verify+push to main, local-first + explicit-tradeoff invariants.

---

# Part 2 — Tasks (what's built and what's next)

Mirror of Part 1 §5. `[x]` = shipped per the prior plan · `[ ]` = open.

## Epic 0 — Scaffold
- [x] **Workspace project docs** — spec/plan/tasks/README/AGENTS/memory.
- [x] **Repo skeleton** — npm workspaces, tsconfig.base, build/test scripts. Repo live: github.com/nekko-labs/hypergate (README splash).
- [x] **`packages/shared`** — core types + daemon API contract.
- [x] **Cross-project rename** — Nekko Notes CLI bin `nekko-mcp` → `nekko-vault-mcp` (that binary belonged to Notes; the name was freed up for this project, which is now Hypergate anyway).

## Epic 1 — Runtime & supervisor (v0.1.0)
- [x] **`RuntimeAdapter` + `ProcessRuntime`** — sandboxed child, allow-listed env, log buffer. · [spec](SPEC.md#31-server-runtime--supervisor-shipped)
- [x] **`Supervisor`** — start/stop/restart/status, logs; e2e test vs an in-repo echo MCP server. · [spec](SPEC.md#31-server-runtime--supervisor-shipped)
- [x] **`DockerRuntime` (opt-in)** — + unit tests (spec only; needs Docker to run live). · [spec](SPEC.md#31-server-runtime--supervisor-shipped)
- [x] **`Supervisor.remove(id)`** — DELETE now drops the instance from the roster entirely (was leaving a ghost `stopped` entry in `list()` and the health count). · Done: 2026-07-04
- [x] **Analytics persistence on disk** — `Supervisor.snapshot()`/`hydrate()` serialize the aggregates + recent-event ring (Maps/Sets → arrays); an `onUsage` hook lets the daemon persist without coupling core to the filesystem. The daemon hydrates `~/.hypergate/analytics.json` on boot and writes it debounced (2s) per call + flush on shutdown, so usage and the "since" start-time survive a restart. Stdio mode stays read-only so a transient spawn never clobbers the file. · [spec](SPEC.md#36-usage-analytics-shipped) · Done: 2026-07-21
- [x] **Remote runtime + MCP OAuth** — a third `RuntimeKind` (`remote`) connects to a hosted HTTP/SSE MCP endpoint instead of spawning a child. The supervisor builds a `StreamableHTTPClientTransport`/`SSEClientTransport` with an injected `authProviderFor` factory; `core/oauth.ts` adds a store-backed `HypergateOAuthProvider` (RFC 8414 discovery + RFC 7591 dynamic registration + OAuth 2.1 auth-code + PKCE, via the SDK's `auth()`), keeping core IO-free (the daemon supplies a file store under `~/.hypergate/oauth/`). New `authorizing` server state + `markAuthorizing()` so a token-less remote server prompts sign-in instead of erroring. Providers without dynamic registration (GitHub) take a static `clientId` (config or `HYPERGATE_CLIENTID_<ID>`). Unit-tested (`oauth.test.ts`, in-memory store). · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Done: 2026-07-23
- [ ] **Crash backoff / auto-restart loop** — auto-restart a `ready` server whose process dies, with capped backoff. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Added: 2026-06-29
- [ ] **Secrets in the OS keychain** — moved to Epic 8; the Rust shell owns it via the `keyring` crate (Credential Manager / Keychain / libsecret), file fallback. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Added: 2026-06-29 · Moved: 2026-07-27
- [ ] **Periodic health checks** — ping each ready server's client on an interval; flip to `errored` + surface in the UI when unresponsive. · Added: 2026-07-04
- [x] **Fix: a stopped server vanished on restart**: `startEnabled()` skipped any server with `enabled: false`, so no supervisor instance existed for it and `/api/servers` (which serves `list()`) omitted it. Stop a server, restart the daemon, and it disappeared from the UI while still sitting in `servers.json`, with no way back but a hand-edit. Found while building the per-agent toggles, where an agent's allow-list could name a configured-but-invisible server. `Supervisor.register(cfg)` now seats a `stopped` instance without spawning or connecting anything (idempotent, so it is safe on every boot) and the daemon calls it for disabled configs. **Verified**: a core test (seated as `stopped` with no tools and no gateway client, no duplicate on re-register, still startable) plus a smoke assertion that stops a server, restarts the daemon, finds it still listed as `stopped`, and starts it again. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Added: 2026-07-28 · Done: 2026-07-28

## Epic 2 — Gateway (v0.1.0 → v0.2.0)
- [x] **Aggregating MCP gateway** — client-per-server, namespaced `server__tool` merge, routing; proven end-to-end in tests. · [spec](SPEC.md#32-gateway-shipped)
- [x] **stdio transport** — `hypergated --stdio`. · [spec](SPEC.md#32-gateway-shipped)
- [x] **Streamable HTTP transport** — `/mcp` on the daemon port; stateless SDK transport with JSON responses (`enableJsonResponse`), fresh transport+gateway per request; `npm run smoke:http` boots the daemon and speaks raw MCP-over-fetch (initialize → tools/list → tools/call → 401 without token). · [spec](SPEC.md#32-gateway-shipped) · Done: 2026-07-04
- [x] **Gateway bearer token** — generated once to `~/.hypergate/gateway-token`, `timingSafeEqual` check, exposed via `/api/gateway` + the UI (masked, reveal/copy); `HYPERGATE_NO_AUTH=1` opt-out. · [spec](SPEC.md#32-gateway-shipped) · Done: 2026-07-04
- [x] **Per-client tokens + per-server allow-list** — named **connected agents**, each with its own gateway token scoped to a per-server allow-list (`'*'` or specific server ids). `createGateway` gained an `allowServer` predicate that hides disallowed servers from `tools/list` and refuses their `tools/call`; the daemon resolves the bearer token to master (full access) or an agent (its scope + name as the analytics caller), persists agents to `~/.hypergate/clients.json`, and serves CRUD at `/api/clients`. UI: a "Connected agents" section (masked token, connect snippet, allowed-servers chips, inline editor). Per-server granularity chosen over per-tool (user decision). · [spec](SPEC.md#32-gateway-shipped) · Done: 2026-07-21
- [ ] **Resources & prompts aggregation** — the gateway currently merges `tools` only; aggregate `resources` + `prompts` the same namespaced way. · Added: 2026-07-04
- [ ] **Live tool-change notifications** — forward `notifications/tools/list_changed` when a managed server starts/stops so connected clients refresh without a manual reconnect. · Added: 2026-07-04

## Epic 3 — Registry & API (v0.1.0)
- [x] **Curated catalog** — filesystem/github/fetch/postgres/nekko-vault + custom server config. · [spec](SPEC.md#33-registry--catalog-shipped)
- [x] **Fly.io catalog entry** — one-click add for the Fly.io MCP server (`flyctl mcp server`, stdio; prompts for `FLY_API_TOKEN`). Verified end to end: added → `ready` → 60 `fly-*` tools aggregated as `fly__*` through the gateway; a `fly__fly-apps-list` call routes to flyctl and returns the auth-required error until `flyctl auth login`. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-07-21
- [x] **Daemon HTTP management API** — list/add/remove/start/stop/restart/logs/gateway + client-config export. · [spec](SPEC.md#32-gateway-shipped)
- [x] **Official MCP registry search** — `core/registry-search.ts` fetches `registry.modelcontextprotocol.io/v0/servers?search=` and maps each hit to a `RegistryEntry` (npm→`npx -y`, pypi→`uvx`, oci→docker image, env vars→prompts; remote-only flagged not-runnable). Daemon `GET /api/registry/search` (8s timeout, soft-fails to `[]`) is the one deliberate outbound call, only on user search. UI: a debounced search box in the Add-server area. Mapper unit-tested (fetch injected); verified live. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-07-21
- [x] **One-click OAuth catalog + daemon flow** — GitHub (`https://api.githubcopilot.com/mcp/`) and Context7 (`https://mcp.context7.com/mcp/oauth`) added as `remote`/`oauth` catalog entries. Daemon: `POST /api/servers` kicks off `auth()` for remote+oauth and returns `authUrl`; `GET /oauth/callback` maps CSRF `state` → server, completes the exchange, and auto-connects (self-contained result page); `POST /api/servers/:id/authorize` (re-auth) + `/disconnect` (sign out, drop tokens). UI: OAuth badge + "🔐 Sign in & add" that opens the login popup, an `authorizing` server row with Sign in / Disconnect, `🌐 remote` chip + host. **Verified:** Context7 works end-to-end zero-config (real `authUrl` with dynamic client + PKCE); GitHub reaches the AS but has no dynamic registration, so it needs a pre-registered client id (surfaced as an actionable message + catalog note), while the grouped catalog also offers token and local connections. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-07-23
- [x] **Remote bearer-token authentication** — GitHub (`https://api.githubcopilot.com/mcp/`) now uses a `remote`/`token` catalog entry. The UI prompts for a fine-grained or classic PAT, the daemon stores it in the existing per-server secret blob, and the supervisor sends it as `Authorization: Bearer` without exposing it in config, status responses, or logs. Added `POST /api/servers/:id/token`, effective OAuth-mode status reporting for the registered-client escape hatch, and token replacement on rejected credentials. **Verified:** real `gh auth token` connects GitHub to `ready` with 41 tools; deletion removes the stored secret; a bogus token remains `authorizing`. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-08-03
- [x] **Vercel OAuth and catalog Add clarity** — curated Vercel's verified `https://mcp.vercel.com` Streamable HTTP endpoint as remote OAuth; matching curated entries now precede live-registry alternatives while searching and duplicate ids or endpoints collapse. Browser mode reserves the auth window synchronously during the click before awaiting the daemon, preventing popup blockers from swallowing the returned URL; the desktop shell keeps its external-browser IPC path. Catalog Add controls use a restrained violet-to-cyan theme border with distinct hover, focus, disabled, and stronger OAuth states. Verified with focused pure tests, full build, 143 JavaScript tests, real isolated Vercel DCR returning `vercel.com/oauth/authorize` with client id and PKCE, and rendered DOM checks at 1360x900 and 620x700 with no horizontal overflow. The live Vercel server is added and awaiting Philip's browser consent; Devin already has the `'*'` server scope, so Vercel becomes available automatically when ready. · [spec](SPEC.md#33-registry--catalog-shipped) · [feature brief](../obsurdian/projects/hypergate/features/vercel-oauth-and-catalog-action/prompt.md) · Added: 2026-08-01 · Done: 2026-08-01
- [x] **Expanded official catalog + trust badges + recommended/popularity sort** — added verified first-party catalog entries (kotrain, supabase, linear, figma + Figma Dev Mode, atlassian, azure, aws, gcp-toolbox, cloudflare + docs, higgsfield, meta-ads); `RegistryEntry` gained `official`/`recommended`/`publisher`/`popularity`. `registry-search` derives `official`/`publisher` from the reverse-DNS namespace (domain-verified ⇒ first-party; `io.github.*` ⇒ community). New `sortRegistry` (recommended set fixed-first, then popularity desc, stable fallback) + `/api/registry/popularity` (npm downloads + GitHub stars, 8s budget, cached 24h to `~/.hypergate/popularity.json`, soft-fail) fetched **lazily when the catalog opens** so nothing hits the network on boot. Framer/YouTube/Nano-Banana intentionally not curated (no runnable first-party config) — still reachable via registry search. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-07-24
- [x] **CLIs section (detect + search command-line tools)** — `core/clis.ts` `KNOWN_CLIS` catalog (runtimes, package runners, docker, cloud CLIs, MCP bins); daemon `/api/clis` (shell-free PATH scan preferring real exts, bounded `--version` probe, 10s memo) + `/api/clis/check?name=` (charset-validated, injection-safe, existence + version). UI: a collapsible "Command-line tools" section on the Servers page with a detected-count, per-tool status/version/path/install-hint, and an ad-hoc availability search. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-07-24
- [x] **Confidential-client (client_secret) support** — GitHub requires client auth at the token endpoint even with PKCE (it doesn't treat native apps as public clients), so a client id alone isn't enough. `HypergateOAuthProvider` now takes a `clientSecret`; when present it's returned in `clientInformation()` and `token_endpoint_auth_method` flips to `client_secret_post` (the SDK negotiates post/basic from the server's advertised methods). Daemon resolves it from config or `HYPERGATE_CLIENTSECRET_<ID>` (env-only, never the catalog). DCR providers (Context7) stay pure public clients, unchanged. Verified: with `HYPERGATE_CLIENTID_GITHUB`+`_CLIENTSECRET_` set, adding GitHub bypasses DCR and yields a real `github.com/login/oauth/authorize?client_id=…&code_challenge=…` URL. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-07-24
- [ ] **Register + ship the Hypergate GitHub OAuth app** — create the app (org `nekko-labs`, callback `http://localhost:7777/oauth/callback`), then inject its id+secret at release (env, not source) so GitHub is one-click for every user like Context7. One shared app, not per-user. Needs Philip. · Added: 2026-07-23
- [ ] **Registry background sync / caching** — periodic snapshot sync + offline cache (beyond on-demand search). · Added: 2026-07-21
- [ ] **Import existing configs** — one-click import of `.mcp.json` / Claude Desktop / Cursor server configs into managed servers. · Added: 2026-07-04

## Epic 4 — UI
- [x] **`apps/web` standalone UI** — list/status/add-from-catalog-or-custom/start-stop/restart/logs/tools/runtime-picker/gateway copy. · [spec](SPEC.md#34-ui-shipped)
- [x] **Served by the daemon** — built UI served at `/` (path-sanitized static handler); one port for UI + API + gateway. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-04
- [x] **Fresh competitive design** — violet→cyan Nekko brand (Kotrain palette era): sticky header with daemon-status pill, hero + stat chips, gateway card (endpoint copy, masked token reveal/copy, snippet tabs for Claude Code / `.mcp.json` / stdio / Kotrain), server cards with status pills + expandable tool chips + logs, catalog grid with requires-chips + custom-server card, isolation segmented control in the add form, empty states, light+dark via `prefers-color-scheme`. Verified headless (screenshot). · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-04
- [x] **Branded favicon** — `apps/web/public/favicon.svg` (violet→cyan rounded-square + white cat mark) replaces the 🐾 emoji data-URI; served by the daemon and copied into `dist/` by Vite. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-21
- [x] **Tool inspector (list view)** — a server's tools now expand into a clickable list; each tool reveals its description + parameters (name/type/required) parsed from its `inputSchema`. `ServerStatus` gained `toolDetails: ToolInfo[]` (additive to the `tools` names), captured by the supervisor from `client.listTools()`. The add-server form also gained a Docker **image** field (and the daemon add-validation now accepts command *or* image). · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-21
- [ ] **Live log streaming** — stream logs (SSE or poll-tail) instead of a snapshot on click. · Added: 2026-07-04
- ~~**Extract `@hypergate/ui`**~~ — parked: the Kotrain integration shipped natively against the daemon API (simpler than a cross-repo package); revisit only if a deeper embed is wanted. · Parked: 2026-07-04

## Epic 5 — Kotrain integration (v0.2.0)
- [x] **Kotrain ↔ Hypergate** — shipped in the kotrain repo (then open-paw; branch `feat/hypergate-integration`): the Kotrain MCP client gained a **streamable-HTTP transport** (`McpServerConfig.url` + `token`, JSON/SSE reply parsing, session-id echo), a host-side `detectNekkoMcp()` probe (`mcp:nekko` channel through the five-touch chain), and a Settings card that shows a detected daemon with one-click **Connect gateway** + **Open manager** (workbench browser pane). Proven by `scripts/itest-mcp-http.mjs` (Kotrain host → gateway → echo server round trip + 401 on a bad token). · [spec](SPEC.md#35-integrations-shipped) · Done: 2026-07-04

## Epic 6 — Distribution (later)
- [x] **Windows tray launcher** — `scripts/hypergate-tray.ps1` (+ `.cmd`, `npm run tray`): a system-tray/taskbar icon (GDI+ gradient-cat, no .ico asset shipped) that keeps the daemon running and offers Open manager / Restart / Quit (double-click opens the UI). Interim desktop presence before the Electron shell. Verified: parses + draws, launches detached and stays alive (message loop up), starts a healthy token-auth daemon, Fly auto-starts from persisted config. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-21
- [x] **npm publish `hypergate` / `hypergated`**: packaging built and proven end to end; see Epic 9. Publishing itself needs Philip's `npm login`. · Added: 2026-07-04 · Done: 2026-07-27
- ~~**Electron shell**~~ — superseded by the Rust shell in Epic 8 (see §1.1). · Superseded: 2026-07-27
- [ ] **docker-compose · signed releases · GitHub Actions CI** (paused like Notes until launch). Signing: Azure Trusted Signing for Windows, Apple Developer notarization for macOS. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-06-29

## Epic 7 — Hypergate rebrand + marketing site (v0.7.0)
- [x] **Rename NekkoMCP → Hypergate** (whole product + codebase; hypergate.app purchased): packages `@nekko-mcp/*` → `@hypergate/*`, daemon `nekko-mcpd` → `hypergated`, env vars `NEKKO_MCP_*` → `HYPERGATE_*`, data dir `~/.nekko-mcp` → `~/.hypergate` (one-time boot migration adopts the old dir wholesale so servers/tokens/analytics/OAuth grants survive), tray `nekko-tray.*` → `hypergate-tray.*`, localStorage `nekko-theme` → `hypergate-theme`, `NekkoOAuthProvider` → `HypergateOAuthProvider`, GitHub repo `nekko-labs/nekko-mcp` → `nekko-labs/hypergate`. New warp-gate mark (gradient ring + glowing core) across favicon, tray icon, and a redesigned deep-space splash. Kept: Nekko Labs (the company), the Nekko Vault catalog entry, and Kotrain (then open-paw) identifiers (`detectNekkoMcp`, `mcp:nekko`) that live in the other repo. Verified: build + 37 tests + both smoke suites green, daemon boots, migration exercised for real, old gateway token rotated. · [spec](SPEC.md#1-vision--positioning) · Done: 2026-07-25
- [x] **Marketing site (`apps/site`) + deploy**: static Vite + TypeScript one-pager with Lenis smooth scroll (anchor easing; velocity feeds the visuals), a WebGL liquid warp-gate hero (fbm domain-warp fragment shader, scroll-boosted, CSS fallback), a canvas starfield that streaks into warp lines on fast scroll, reveal-on-enter, screenshot parallax, and six feature sections each backed by a **real captured screenshot** of the running app (Playwright, dark theme, real servers + 26 genuine gateway calls behind the analytics shot). Deployed to Vercel (nekkolabs/hypergate); `hypergate.app` + `www` attached, registrar DNS still on parking, live alias hypergate-psi.vercel.app until the A records land. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-07-25
- [x] **Refresh the marketing screenshots for the current app**: recaptured all six feature views from the v1.3 left-navigation manager at 1360 × 850 using an isolated daemon, scoped demo agents, four demo servers, and 26 real gateway calls. Rebuilt full and 680px PNG/WebP variants, and updated every image description to match what the screenshot now shows. No personal config, paths, tokens, or credentials are present. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-15

## Epic 8 — Durable store + Rust desktop shell (v0.8.0)

Sequenced deliberately: the store lands first because logging and usage metering both need it, then the shell, then the two native capabilities the shell unlocks. See §1.1 for the stack decision behind this epic.

- [x] **SQLite store (`apps/daemon/src/store.ts`)** — `node:sqlite` in WAL mode at `~/.hypergate/hypergate.db`. Append-only `usage_events` (durable, unbounded, aged off not counted off) plus rolled-up `agg_*`/`totals` tables so boot is O(distinct servers × tools + clients) instead of a replay. Durable `server_logs`. Batched inserts off the gateway hot path; `analytics.json` imported once then moved aside as `.migrated`. New `GET /api/usage/events` (server/client/since filters) and a 24h series bucketed in SQL. `Supervisor` gained `onUsage(event)` + `onLog(serverId, line)` hooks, keeping core IO-free. `--stdio` deliberately skips the store, since absolute aggregate writes from a transient spawn would stomp the resident daemon's counts. **Verified:** 13 new store unit tests (round-trip, 2500-event history past the old 2000 cap, filters, SQL series with empty hours, log scoping, retention incl. `=0` meaning forever, legacy import + no double-import, corrupt-snapshot tolerance, idempotent flush), plus smoke assertions that history and the series survive a hard `TerminateProcess` kill via WAL recovery. · [spec](SPEC.md#36-usage-analytics-shipped) · Done: 2026-07-27
- [x] **Rust tray shell (`apps/shell`)** — `tray-icon` + `muda` + `tao`, one 2.7MB binary. Menu: live status header (servers + call count, refreshed off-thread every 4s), Open manager, Start/Stop all servers, Restart daemon, Start-at-login checkbox, Quit. Supervises the daemon as a child (draining its stdio so a full pipe buffer can never block it) and reaps it on exit, or attaches to a running daemon and leaves it alone. Single-instance via a bound loopback port, so a second launch opens the UI instead of adding a duplicate icon. Menu events are pushed through an `EventLoopProxy` rather than polled, so clicks have no perceptible latency. macOS: template icon + `ActivationPolicy::Accessory`. Menu-only interaction everywhere, because Linux SNI delivers no click events. The warp-gate icon is generated as RGBA in code (3×3 supersampled), so no asset ships. Retired `scripts/hypergate-tray.ps1`/`.cmd`. **Verified:** tray launches, logs `tray icon registered` (a successful `Shell_NotifyIcon` NIM_ADD), boots a healthy daemon, reaps it on exit, and a second instance defers and exits 0 without disturbing the first. 5 icon unit tests cover shape, gradient, transparency and the template variant. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-27
- [x] **Autostart on all three platforms** — Windows `HKCU\...\Run`, macOS `~/Library/LaunchAgents` plist (+ `launchctl load -w`), Linux XDG `~/.config/autostart` (spec-correct `Exec` escaping). Owned by the shell; the daemon delegates via `hypergate autostart`, so `/api/settings` reports the truth on every OS instead of `startupSupported: false`. `reg.exe`/`launchctl` are always invoked with explicit argv, never a shell string, so a path with spaces or quotes cannot become injection. **Verified:** `autostart on` writes `"…\hypergate.exe" tray` to the real Run key, `status` reads it back, `off` removes it; the daemon reports `startupSupported: true` on win32. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-27
- [x] **`hypergate` CLI** — clap subcommands (`tray`, `start`, `stop`, `restart`, `status`, `list`, `logs`, `open`, `gateway`, `autostart`, `secret`, `sandbox-exec`), all clients of the daemon's existing HTTP API. `stop` uses a pid file rather than a daemon shutdown route on purpose: the management API is unauthenticated on localhost, so a kill switch reachable from any local process would be worse. **Verified:** `status` reports the running daemon + usage + keychain (and exits 0, not 1, when the daemon is down), `list` shows state, `gateway --token-only` matches the daemon, `logs` streams the durable log. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-27
- [x] **Secrets in the OS keychain** — `keyring` 4 (Credential Manager / Keychain / Secret Service) behind `hypergate secret get|set|delete|check`; `set` reads from stdin, never argv. The daemon delegates via subprocess rather than linking a native library: the gateway token is adopted into the keychain before the daemon starts and passed as `HYPERGATE_TOKEN` (already supported), so no plaintext file is ever written; each remote server's OAuth grant is one keychain entry holding the whole blob, cached in memory, so a boot costs one subprocess per server rather than one per key. Existing plaintext files are migrated on first read then deleted. Falls back to the old file behaviour with no keychain. `HYPERGATE_KEYCHAIN_NAMESPACE` scopes entries, since a keychain is machine-global and `HYPERGATE_DIR` cannot isolate it. Moved here from Epic 1. **Verified:** real Credential Manager round-trip (set/get/delete, exit 1 on a missing key), daemon token matches the keychain entry, no `gateway-token` file on disk, and the token is read back unchanged across a restart. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Done: 2026-07-27
- [x] **`sandbox-exec` launcher (real resource limits)** — a per-server `limits` block (`memMb`/`cpuPct`/`nofile`) makes the supervisor spawn `hypergate sandbox-exec … -- <cmd>`. Windows: Job Object with `JOB_OBJECT_LIMIT_JOB_MEMORY`, CPU rate hard cap, and `KILL_ON_JOB_CLOSE` (which fixes grandchildren leaked when an `npx` shim is killed). macOS/Linux: `setrlimit` in `pre_exec` + a dedicated process group. stdio inherited, so the MCP stream is untouched. Unappliable limits warn, or fail under `--strict`, instead of silently no-op'ing; a missing launcher logs `UNSANDBOXED` rather than implying limits are in force. Two bugs found and fixed while building it: the job handle must be leaked (a guard's `Drop` closed it and `KILL_ON_JOB_CLOSE` then killed the launcher before it could report the child's exit code), and `exec` cannot be unit-tested in-process because it sandboxes its own caller. **Verified:** 11 integration tests against the real binary (exit-code propagation, stdout/stderr separation, memory limit, CPU cap, strict-mode refusal, missing program) + 7 core unit tests for the wrapping, and a smoke test where a real MCP server starts through the launcher, reaches `ready`, and serves a `tools/call` round trip. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Done: 2026-07-27
- [x] **Shell bridge smoke suite** — `scripts/smoke-shell.mjs` (`npm run smoke:shell`) covers the seam unit tests cannot: the daemon delegating to the real binary for keychain and login-item work, a server launched through `sandbox-exec` serving MCP traffic, the CLI as an API client, and durable state surviving a restart. Also `npm run shell:build` / `shell:test` / `tray`. · Done: 2026-07-27

## Epic 9: install without building + a complete CLI (v0.9.0)

The theme: someone who has never seen the repo types one command and has a working gateway, then drives all of it from the terminal.

- [x] **CLI: manage servers**: `catalog`, `search`, `add`, `rm`, and `server start|stop|restart`, all clients of the daemon's existing HTTP API (new `commands.rs`; `api.rs` gained POST-with-body, DELETE, and status-aware error reporting so a 409 says `id_exists` instead of "HTTP 409"). `add` resolves a curated catalog id first, then an exact id from the official registry, then falls back to a custom `--command`/`--image`/`--url`; it mirrors the daemon's own validation locally so a mistake names the flag that would fix it, fills a `requires` key from the environment when it isn't passed as `--secret`, and opens the browser for a remote OAuth entry. The merge/parse logic is pure and unit-tested (8 tests) so none of it needs a running daemon. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-27
- [x] **CLI: drive the gateway**: `tools [--server id]` and `call <server__tool> [json] [--arg k=v]` speak JSON-RPC to `/mcp` with the bearer token, deliberately taking the same path a connected agent does rather than an internal shortcut, so the CLI verifies the real gateway. `--arg` values parse as JSON when they can (so `count=3` is a number) and stay strings otherwise. A tool that reports an error exits non-zero. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-27
- [x] **npm packaging (`hypergated` + `hypergate-shell-<os>-<arch>`)**: `scripts/build-npm.mjs` bundles the daemon into a single file (esbuild; core, shared and the MCP SDK inlined, so installing pulls no dependency tree) with the built web UI beside it, and emits the main package plus one package per platform binary. A JS shim execs the binary matching `process.platform`/`arch` and hands it `HYPERGATE_DAEMON_CMD` so the two halves find each other. Optional dependencies guarded by npm's `os`/`cpu`: unsupported platforms still install a working daemon. Two supporting fixes: `HYPERGATE_DAEMON_CMD` now accepts a JSON array (the whitespace form tore `C:\Program Files\…` in half) and the daemon now honours `HYPERGATE_PORT`, which previously only the shell read, so setting it moved the CLI without moving the daemon. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-07-27
- [x] **Windows: a detached daemon no longer holds its launcher's pipes**: `CreateProcess` inherits every inheritable handle, not just the ones in `STARTUPINFO`, so `hypergate start` left the daemon holding the stdout/stderr pipes of whatever launched it. Any caller that captured output (a script, CI, `execFileSync`) then blocked waiting for an EOF that would only arrive when the daemon exited. Found by the packaged-install smoke, which hung for its full 180s timeout; fixed by clearing `HANDLE_FLAG_INHERIT` on our own std handles before spawning. · Done: 2026-07-27
- [x] **`npm run smoke:install`**: packs the real tarballs, installs them into a clean throwaway project, and drives the *installed* CLI end to end: version, status-while-down, start, the packaged web UI over HTTP, catalog, add a real stdio MCP server, list, tools through the gateway, call it, a failing call, stop/start, gateway info, rm, stop. Nothing in it touches the repo's build output at runtime, so "a user can install and run this" is a test rather than a claim. · Done: 2026-07-27
- [x] **CI + release workflows**: CI: build + unit tests on Node 22 and 24, the HTTP and feature smokes, `cargo fmt`/`clippy`/`test` on Linux, macOS and Windows, and the packaged-install smoke. Release: tagging `v*` cross-builds the shell for six targets (win32/darwin/linux x x64/arm64), publishes the platform packages **before** the main package (whose optional deps pin their exact version) to npm with provenance, and attaches standalone binaries to the GitHub release. Unblocks the "signed releases · GitHub Actions CI" item in Epic 6, which had been paused since launch. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-07-27
- [x] **Registry search returns one row per server**: the official registry returns a row per *version*, so a server with five releases appeared five times, which is noise in the UI and outright ambiguous for `hypergate add <id>`. Deduped by id, keeping the newest. · Done: 2026-07-27
- [x] **Desktop launcher (`hypergate shortcut install`)**: the thing that makes Hypergate clickable rather than command-line-only. Windows creates a Start Menu `.lnk` (and `--desktop` a desktop icon) via `IShellLinkW`/`IPersistFile`, targeting the native binary with `tray`, with a working directory of `$HOME` and an icon from a real multi-resolution `.ico`. macOS writes a `Hypergate.app` in `~/Applications` (Info.plist with `LSUIElement`, a `/bin/sh` stub that execs the installed binary so an upgrade needs no re-install, single-quoted against paths with spaces). Linux writes an XDG desktop entry plus a scalable SVG in the hicolor theme. All per-user, no elevation, `shortcut uninstall` reverses it. Two details that matter: folder locations come from `SHGetKnownFolderPath`, not `%USERPROFILE%\Desktop`, which on this machine resolved to the OneDrive-redirected desktop (a hand-built path would have been invisible to the user); and the tray now closes the console Windows opens for a shortcut launch, detected via `GetConsoleProcessList` returning 1, while keeping it when launched from a terminal. `icon.rs` gained size-parametric rendering, an `.ico` writer (uncompressed 32-bit BMP payloads at 16/32/48/256, so no deflate dependency) and an SVG writer. **Verified:** `LoadImage` loads the `.ico` at all four sizes; the `.lnk` reads back with the right target/args/icon/working dir; clicking it from Explorer starts the tray with no conhost attached, the daemon healthy, and a second click defers to the running instance instead of duplicating the icon. 9 new/updated unit tests. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-27
- [x] **Runtime-free daemon (Node SEA)**: `scripts/build-standalone.mjs` compiles the daemon into one executable by injecting the esbuild bundle into a copy of the Node binary, and assembles the install tree (`hypergate`, `hypergated`, `web/`, docs) that all three installers package verbatim. **Bun was tried first and rejected**: `bun build --compile` is one command and cross-compiles, but Bun 1.3.6 has no `node:sqlite`, so `openStore` returned undefined and the daemon silently degraded to in-memory analytics, losing durable usage history and server logs. Verified by booting it and checking for `hypergate.db`, which is exactly the failure that would otherwise have shipped. Two supporting changes: the daemon resolves its web UI relative to `process.execPath` (there are no module paths inside a compiled binary), and its three top-level `await`s became promise chains, because a SEA entry point must be CommonJS and CJS cannot express top-level await (ordering guarantees unchanged: boot still completes before `listen`). · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-07-27
- [x] **Native installers for all three platforms**: NSIS `-setup.exe` (per-user under `%LOCALAPPDATA%`, `RequestExecutionLevel user`, HKCU-only Add/Remove Programs entry, optional desktop icon / PATH / start-at-login components, and a stop-before-overwrite step so upgrading over a running tray works), macOS drag-to-Applications `.dmg` (`Hypergate.app` as an `LSUIElement` agent with both binaries and the UI inside it; launching it creates per-user CLI links), and Linux `.deb` + `.rpm` + tarball (everything under `/usr/lib/hypergate` with `/usr/bin` symlinks, which is what keeps the shell's sibling-daemon lookup and the daemon's UI lookup both working; GTK is a hard dependency because the tray links it at load time and it is the same binary as the CLI). Each is built on its own OS, never cross-built. The installers call `hypergate shortcut install`, `autostart on` and `icon` rather than reimplementing them, so there is one source of truth for where a launcher goes. Uninstalling leaves `~/.hypergate` alone. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-07-27
- [x] **`hypergate icon <path>`**: writes the mark as `.ico` or `.svg`, chosen by extension, so a packaging script can get the brand asset out of the binary instead of the repo carrying a checked-in copy that drifts. · Done: 2026-07-27
- [x] **`npm run smoke:standalone`**: boots the compiled daemon with no Node involved, asserts the version, that the web UI is served from beside the executable, that the SQLite store opened, that a managed MCP server launches, and that a tool call routes through the gateway. Runs in CI (daemon-only, so no cargo build) and in the release build before an installer is uploaded. · Done: 2026-07-27
- [x] **`build-artifacts.yml`**: the six-way platform matrix as a reusable workflow, called by the release and runnable on demand, so an installer can be built and tested without cutting a release. · Done: 2026-07-27
- [ ] **Publish to npm**: needs Philip's `npm login` (or an `NPM_TOKEN` repo secret for the release workflow). Everything else is done and verified locally against real tarballs. · Added: 2026-07-27
- [ ] **Verified end to end against Kotrain**: the curated `kotrain` catalog entry, added through the installed CLI, reaches `ready`, aggregates 9 tools as `kotrain__*`, and `hypergate call kotrain__kotrain_status` (tool names were `open_paw_*` at verification time, then `nekkos_*`, and `kotrain_*` upstream since 2026-08-03) returns this machine's real provider/workspace/session summary through the gateway. Kept as a manual check: it needs a local Kotrain install, so CI uses the in-repo echo server instead. · Verified: 2026-07-27

## Epic 10 — Connect an agent in one click (v0.10.0)

The theme: the gap between "the gateway is running" and "my agent is using it" should be one click, and there should be exactly one place to do it.

- [x] **One connect surface, not two**: the gateway bar's "Connect an agent" disclosure (snippet tabs for Claude Code / `.mcp.json` / stdio / Kotrain) is gone. It duplicated the Connected agents section and handed out the *master* token, which reaches every server and can't be revoked on its own — the opposite of what the per-agent tokens exist for. The bar now shows the endpoint, the master token, and a link down to Connected agents. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-28
- [x] **Per-agent Connect panel**: each agent row expands into a tab per client. A `cli` client (Claude Code, Gemini CLI) gets a one-click button — the daemon runs the client's own `mcp add`; a `config` client (Cursor, VS Code, `.mcp.json`, Kotrain) gets the snippet and the path of the file it belongs in. Either way the exact command is on screen, quoted for the shell you're actually in (PowerShell / cmd.exe / bash, preselected from `process.platform`), so the button never hides what it did. Result banner shows the CLI's own stdout/stderr, so a failure says what the client said. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-28
- [x] **Quick connect from empty**: with no agents yet, the section is a grid of the clients on this machine (detected CLIs highlighted with their version). One click creates the scoped agent named after the client, then installs — nothing to fill in. Re-clicking reuses the agent of the same name instead of stacking duplicates. · Done: 2026-07-28
- [x] **`packages/core/src/connect.ts` + daemon endpoints**: the client table, the argv per client, the config snippets, and the three shells' quoting rules are pure and unit-tested (12 tests, incl. each shell's own escape character and cmd.exe's caret metacharacters). The daemon adds `GET /api/connect/targets` (PATH detection, memoised), `GET /api/clients/:id/connect` (the same targets filled in with one agent's token) and `POST /api/clients/:id/connect` (run it). **The request only selects a row in the built-in table** — the argv is ours and is spawned without a shell, so `{"target":"claude-code; rm -rf /"}` is a 400, not a command. Entry name is the constant `hypergate`, and a previous entry of that name is removed first so re-connecting after a token change works. `.cmd`/`.bat` shims go through `cmd.exe`, which Node otherwise refuses to spawn. · Done: 2026-07-28
- [x] **Verified in the browser**: against a throwaway daemon (`HYPERGATE_DIR` in a temp dir, port 7788) with a fake `claude.cmd` first on `PATH` — so the real Claude Code config was never touched — the empty-state grid detected it (`v9.9.9-fake`), one click created the agent and registered the gateway, and the panel showed the CLI's own output. Also checked: the undetected client disables the button but still shows its command + install hint, the shell tabs re-quote correctly, Cursor's snippet + `~/.cursor/mcp.json` path render, and the POST rejects unknown/non-CLI targets and unknown agents. · Done: 2026-07-28
- [ ] **More one-click clients**: Codex and Windsurf are missing because their HTTP-MCP config shape moves between versions and shipping a wrong one is worse than shipping none. Adding a client is a row in `CONNECT_TARGETS` plus a case in `connectArgv`/`connectSnippet`. · Added: 2026-07-28

## Epic 11: the desktop app + release signing (v0.10.0)

Driven by user feedback: Hypergate should be a real desktop app on Windows, macOS and Linux, not a tray icon that opens a browser tab. Plus two live bugs on Windows (a terminal window flashing every few seconds stealing focus, and the tray menu freezing shortly after startup), both one root cause.

- [x] **Fix: console flash + frozen tray menu (Windows)**: `autostart::is_enabled()` shelled out to `reg.exe`, and the tray called it every 4s **on the UI thread**. From a console-less process every spawn allocated a fresh console window (the flashing terminal + focus stealing), and the blocking `.output()` froze the open menu. Fixed three ways: the HKCU Run key is now read/written **in-process** (`windows` crate registry API, no subprocess and no argv at all), the autostart state rides the `Wake::Status` event computed on the polling thread so the UI thread never blocks, and the remaining console child (`taskkill` in `daemon::kill`) gets `CREATE_NO_WINDOW`. Verified: on/status/off round-trip against the real registry, all 37 shell tests green, live tray running clean. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **Manager window, the desktop app (`window.rs`)**: `wry` 0.55 joins the `tao`/`tray-icon`/`muda` family (exactly the "add the webview later" path §1.1 reserved): a native window titled Hypergate, 64px procedural icon, hosting the daemon-served UI in the OS webview (WebView2 / WKWebView / WebKitGTK). `hypergate app` = tray + window (what every launcher now runs); `hypergate tray` stays headless (what the login item runs). "Open manager" opens/focuses the window, browser fallback if the platform webview is missing; closing the window leaves the agent resident; Quit stays in the menu. Second `app` launch hands off through the single-instance socket (the lock listener now accepts an `open` verb) and exits; a second `tray` launch does nothing. Binary grew 2.7→3.0MB. Verified live on Windows: window renders the full UI, handoff exits 0 with one process, screenshot captured. macOS/Linux compile in CI; Linux packages now depend on `libwebkit2gtk-4.1-0`/`webkit2gtk4.1` and CI installs `libwebkit2gtk-4.1-dev`. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **Launchers + installers open the app**: `shortcut install` writes `app` into the Start Menu/desktop `.lnk`, the `Hypergate.app` stub and the XDG entry; the NSIS finish page runs `app` ("Open Hypergate now"); the .dmg bundle stub execs `app`. Autostart everywhere keeps `tray`. · Done: 2026-07-28
- [x] **Live version chip**: the web UI topbar chip was hardcoded `v0.7` (two releases stale); it now renders the version from `/health`. (Landed inside PR #22 due to a shared checkout; noted in the workspace memory.) · Done: 2026-07-28
- [x] **Release signing pipeline, secret-gated**: `build-artifacts.yml` signs at three points when secrets exist (shell binary → SEA-minted `hypergated` → installer) and skips cleanly when they don't. Windows: `azure/trusted-signing-action@v2` ×3. macOS: temp-keychain cert import, `codesign --options runtime --timestamp` on both binaries, bundle sealed and `.dmg` signed inside `build-dmg.sh` (env-driven), `notarytool submit --wait` + staple in the workflow; the standalone smoke deliberately runs **after** signing so a signature that breaks the binary fails CI, not a user machine. `release.yml` gains `secrets: inherit` + always-on `SHA256SUMS` with a GPG detached signature when the key exists. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **Linux signing is live**: generated the ed25519 release key (`Nekko Labs Release Signing <releases@nekkolabs.com>`, fpr `94C9E438A4DD9E1A5752105696C248A969F7B919`) in a throwaway keyring, set it as the `GPG_PRIVATE_KEY` repo secret without it ever touching the transcript or disk outside the temp dir, committed the public half to `docs/release-signing-key.asc`, destroyed the keyring. · Done: 2026-07-28
- [x] **docs/signing.md**: the full signing story + Philip's next steps. Key finding: Azure Artifact Signing (né Trusted Signing) dropped the 3-year-history rule at GA but public trust is **US/CA/EU/UK-only**, which excludes Nekko Labs (Japan), so the recommended Windows path is **SignPath Foundation** (free for OSS), with the Azure wiring ready if an eligible entity appears. macOS needs Apple Developer enrollment (secrets enumerated, `provision-keys` skill covers the plumbing). · Done: 2026-07-28
- [ ] **Windows signing account**: apply to SignPath Foundation (or onboard Azure via an eligible entity), then swap/enable the signing steps. Needs Philip. · Added: 2026-07-28
- [ ] **Apple Developer enrollment + certs**: enroll, mint the two Developer ID certs + notary API key, set the seven `MACOS_*`/`APPLE_*` secrets (docs/signing.md steps 1–4). Needs Philip. · Added: 2026-07-28
- [ ] **In-app updater** on top of the signed installers. · Added: 2026-07-28

## Epic 12: turning things off (v0.11.0)

Two asks from Philip, both about *revoking* something the UI could only grant: stop the daemon, and take one MCP server away from one agent.

- [x] **Enable/disable a server per agent, in one click**: `packages/core/src/agents.ts` (`isServerAllowed`, `setServerAllowed`) owns the arithmetic, pure and unit-tested (9 tests); the daemon exposes it as `POST /api/clients/:id/servers/:serverId` with `{allowed}`. Deliberately a single-server flip rather than a whole-allow-list `PATCH`: disabling a server for an agent scoped to `'*'` has to write out the servers that exist *now* minus that one, and only the daemon knows the roster, and a UI computing it from a stale list would silently revoke a server it hadn't heard of. Two decisions worth keeping: enabling never collapses a full list back to `'*'` (that would also grant every future server, which nobody asked for), and revoking an id the roster no longer has is allowed so a stale entry can be cleared, while granting one 404s. The agent object is mutated in place, so the stateless gateway honours it on the very next request. · [spec](SPEC.md#32-gateway-shipped) · Done: 2026-07-28
- [x] **Both sides of that permission in the UI**: the agent row's allowed-servers chips became the control (`✓ Echo` / `✕ Notes vault`, click to flip, error surfaced inline), and each server row gained an **Agents n/m** panel with a switch per connected agent, which is the direction you actually think in after adding a server. The wildcard-pinning consequence is stated in the panel rather than discovered later, and the count goes amber when no agent may use the server. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-28
- [x] **Stop the daemon from the UI**: `POST /api/shutdown` + a two-step Stop control in Settings. The route is the one whose whole job is destructive, so it is guarded by the **master** gateway token *and* a same-origin check. The management API answers with `Access-Control-Allow-Origin: *`, so an unguarded kill switch would be reachable from any page the user visits (exactly the reason the CLI's `stop` used a pid file, per Epic 8). It answers before exiting, then stops listening, closes every managed server (their children are ours; exiting first leaks a process tree), flushes the store and exits 0. `enabled` flags are untouched, so a restart brings the servers back. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Done: 2026-07-28
- [x] **`hypergate stop` after a self-exit**: the daemon can now exit on its own, which leaves the shell's pid file pointing at nothing; `taskkill` on that pid reported a failure for a daemon that was already down. `daemon::stop()` now checks `/health` first, clears the stale file, and reports "nothing to stop", which also unblocks the tray's Restart-daemon path when it had attached rather than spawned. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **Verified**: build + 67 core / 13 daemon / 37 shell tests green, `cargo fmt`/`clippy` clean, `smoke:http` green. `smoke:features` gained six assertions: disabling a server hides it from that agent's `tools/list` and refuses the call, re-enabling restores it, a `'*'` agent gets pinned to the rest, unknown-server grant 404s / revoke succeeds, shutdown refuses no-token + an agent token + a foreign origin, and the daemon really exits 0 after accepting one. In the browser against a throwaway daemon (temp `HYPERGATE_DIR`, port 7799, two echo-backed servers, one `'*'` agent + one scoped): chip and switch both flipped the permission and the real gateway's `tools/list` followed, Stop reported "2 managed servers stopped with it" and the process exited 0 leaving no orphaned children, a restart brought both servers back `ready`, and `hypergate stop` then printed "No daemon running" (exit 0) instead of a taskkill error. · Done: 2026-07-28
- [ ] **Per-tool permissions**: granularity is per-server by design (Epic 2, Philip's call). Revisit only if "this agent may call `read` but not `write`" comes up for real. · Added: 2026-07-28

## Epic 13: knowing there's an update, and taking it (v0.12.0)

The theme: the app should know when it is out of date and be able to fix that itself. Note the ground truth this was built on: **nothing is published yet** (no npm package, no GitHub release), so every piece had to behave sensibly with an empty feed rather than only in the happy case.

- [x] **`packages/core/src/update.ts`** (pure, 14 tests): `compareVersions` (semver-ish, tolerant of a `v` prefix and a missing patch, prereleases ranked below their release, numeric identifiers below alphanumeric per semver §11, junk sorted *below* everything so a garbled feed can never look like an upgrade), `isNewerVersion`, `detectInstallChannel` (npm · installer · repo · unknown, from the daemon's own module path + `process.execPath`, ordered so a checkout living in a folder called `hypergate` is still a checkout), `updatePlan` (what updating takes per channel), and the two feed mappers. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **Daemon endpoints**: `GET /api/update` (cached, never fetches) and `POST /api/update/check[?force=1]` (npm registry, else the GitHub release; 8s budget; cached 24h to `update.json`; soft-fails to what we last knew). Both feed URLs are env-overridable, which is what lets the smoke test run against a stub and an enterprise mirror work at all. `POST /api/update/apply` is guarded like `/api/shutdown` (master token + same-origin) and delegates to the shell. **The invariant holds**: nothing is fetched on boot or on a timer; the UI asks once when it opens, which is the same rule catalog popularity already follows (SPEC §1). · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **`hypergate update [--apply]`** (`apps/shell/src/update.rs`): the part that cannot live in the daemon, because it replaces the files the daemon and tray run from. It writes an updater to the OS temp dir, spawns it detached, then stops the agent (a `quit` verb added to the tray's single-instance socket, else the daemon's own API). The updater waits for the daemon port and the tray lock to go quiet (Windows releases file handles a beat late, so there is a deliberate grace pause), runs `npm install -g hypergated@<v>`, relaunches `hypergate app`, and logs every step to `~/.hypergate/update.log`. The updater is **JavaScript run by the node an npm install necessarily has**, rather than twin `cmd`/`sh` scripts: it needs real logic, and one implementation beats two. The version is re-validated against a strict charset before it reaches a command line, and the command is rebuilt locally from the channel rather than executed as a string handed over HTTP. Two tests: the charset guard, and `node --check` on the generated updater (which also proves a Windows path survives JSON-encoding into it). · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-28
- [x] **One-click limited to the npm channel, on purpose**: the native installers are unsigned on Windows and macOS until the signing work lands, and silently downloading and running an unsigned installer is worse than showing the release page; the Linux packages need root, which a per-user agent should not acquire. Those channels get the exact command; a checkout is told to pull and rebuild. The follow-up (auto-download the *signed* installers) is now the "signed update feed" item in Epic 11. · Done: 2026-07-28
- [x] **UI**: the topbar version group grew an update state immediately to its right (**↑ v0.13.0** button when there is one, a quiet **latest** when there isn't, because silence reads as "hasn't checked") plus a **Check for updates** button revealed on hover, on keyboard focus, and unconditionally where hover doesn't exist. Settings opens with an **Updates** section: versions, channel, last check, release notes, **Update now** (or the command, with Copy), and what pressing it will do. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-07-28
- [x] **`npm run smoke:update`** (in CI beside the packaged-install smoke, reusing its build): copies the real packaged output into a `node_modules` layout, points the daemon at a stub feed serving 99.0.0, and proves the whole chain: the npm channel is detected, `GET /api/update` touches nothing, a check finds the version and names the command, the answer is cached for a day and `force` overrides it, apply refuses an anonymous caller and a foreign origin, and an accepted apply really stops the daemon (exit 0) and runs `npm install -g hypergated@99.0.0` against a **fake npm that records its argv**. Nothing is published, so a genuine install cannot be tested; this tests everything up to the package manager's front door. · Done: 2026-07-28
- [x] **Verified beyond the smoke**: 82 core / 13 daemon / 39 shell tests, fmt + clippy clean, the other smokes green. In the browser against a packaged npm-channel daemon and a switchable stub feed: the chip reads `↑ v99.0.0` and the Updates section offers **Update now** with `npm install -g hypergated@99.0.0`; flipping the feed to the current version turns the chip into `latest` and the section into "You're on the latest version · checked just now". Against the **real** feeds today it reports no published release, with no error, which is the truth. Screenshots still time out on this machine, so the UI was read through the DOM/accessibility tree (and the hover reveal through CSS rule enumeration). · Done: 2026-07-28
- [x] **Update failures are visible**: the shell logs its own decisions to `update.log`, not just the updater's output. Found while building this: the daemon was passing a `--yes` flag the CLI didn't define, so the spawned updater died on a clap error with its stdio discarded, i.e. completely silently. The log is what turned that from a mystery into a one-line fix. · Done: 2026-07-28
- [ ] **Update from the tray**: the tray's status header could say "update available" and offer it, for someone who never opens the manager. · Added: 2026-07-28

## Epic 14: two lines to a running gateway (v0.13.0)

Philip, on the install block: *"this is too many commands… it should be one line to download and one line to start. I'm thinking `hypergate start` should kick it off."* Plus the other half of the same complaint: with a copy already installed, how you update it was not written down anywhere.

- [x] **`hypergate start` is the setup**: it starts the daemon (as before), creates the launcher on the **first run only**, and opens the manager. Four commands become two. Each half is switchable (`--no-open`, `--no-shortcut`, `--desktop` for a desktop icon now rather than just first run), and the whole thing is skipped on CI, over SSH, and on a Linux box with no `DISPLAY`/`WAYLAND_DISPLAY`, so the same command is right on a headless box. Two decisions worth keeping: the first-run test is a **stamp file** (`~/.hypergate/launcher.stamp`), not the launcher's existence, because someone who ran `shortcut uninstall` or binned the icon has expressed a preference and `start` putting it back every time would be a bug; and the stamp is written **before** the attempt, so a machine where a launcher cannot be made (no COM, a read-only home) does not retry on every single start. A launcher that fails is reported, never fatal: the daemon is up, which is what was asked for. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-30
- [x] **Headless is a pure function**: `commands::is_headless(platform, var)` takes its environment as a closure, so every case is a unit test instead of a race between tests setting real env vars in one process. `HYPERGATE_HEADLESS=1|0` overrides it outright, because no heuristic covers every container, remote desktop and CI runner, and a user told "we guessed wrong" needs something to say back. · Done: 2026-07-30
- [x] **Updating is findable**: the machinery shipped in v0.12.0 and nothing pointed at it. `hypergate status` gained an Update line in all three states (available, up to date, never checked), `hypergate start` repeats an available one from the daemon's **cache** (`GET /api/update` never fetches, so a start path stays offline and fast), and both READMEs plus `llms.txt` gained an Updating section that says what `--apply` actually does and why it is npm-only. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-07-30
- [x] **Verified**: 31 unit + 12 integration shell tests (four new: the headless matrix and the override, plus `update`/`shortcut` added to the CLI-surface test), `cargo fmt`/`clippy` clean, `smoke:install` driven with `--no-open --no-shortcut` so a smoke run cannot open a browser tab or leave a Start Menu entry on the machine running it. · Done: 2026-07-30
- [x] **Nothing is published yet** — resolved in Epic 16 (v0.15.0). Both faults are named there: no release existed, and the release job could not create one without an `NPM_TOKEN`. The updater now installs from the release assets, so a publish is no longer what stands between a user and an update. · Done: 2026-08-01

## Epic 15: the desktop app, as an app (v0.14.0)

Philip, on the manager and the tray: the add-agent button should offer the agents people actually use ("kotrain, claude code, cursor, devin, hermes, odysseus, and openclaw for example") instead of listing every client's connect options under *every* agent; the Edit button should go; a signed-in MCP server should say which account it is signed in as; "run on startup" should actually work; "daemon up" should say **server healthy** and let you stop it on hover; double-clicking the tray icon should open the app; and closing the window should shrink to the tray — asking, the first time, whether that is what closing means.

- [x] **The agent catalog replaces the tab strip**: `CONNECT_TARGETS` in `@hypergate/core` is now the list "+ Add agent" offers, so choosing a harness and connecting it are one decision instead of two. Ten entries with a blurb and live detection — Claude Code, Cursor, Kotrain, OpenClaw, Gemini CLI, Hermes, VS Code, `.mcp.json`, Odysseus, Devin — plus Custom agent. `AgentClient.target` records which one an agent *is*, and the connect panel shows that client alone. The formats are the real ones, checked against each vendor's docs: OpenClaw takes `openclaw mcp add … --transport streamable-http --header` (and nests under `mcp.servers`), Kotrain' `mcpServers` is an **array** whose entries carry their own `token`, Hermes is YAML at `~/.hermes/config.yaml`. Two new methods where "run a command" and "paste a file" both fail: `manual` hands over the endpoint + header for a client whose MCP list lives in a UI (Devin, Odysseus), and every `cli` target now also carries a snippet, so a machine without the CLI is no longer a dead end. Devin says out loud that a cloud agent cannot reach `localhost`, which is a ten-minute mistake otherwise. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Edit is gone; the name tells the truth**: an official agent renders its name as text with an `official` chip and the daemon **409s** a rename — a row that says "Cursor" and holds Cursor's token should not be able to say something else. A custom agent's name is an input in the row itself (Enter or blur saves, Escape abandons, a failed save reverts and marks the field). Permissions were already inline chips, so the button had nothing left to edit. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Which account is this server signed in as**: `ServerStatus.account`, filled for remote OAuth servers. `packages/core/src/account.ts` is the free half — decode the `id_token`, else a JWT access token, prefer email → username → name → subject — and is deliberately signature-free, because these are our own stored grants being read to label a row, not credentials being authenticated. When the token is opaque (Linear, Figma, plenty of others) the daemon walks the discovery chain the OAuth flow already used: `/.well-known/oauth-protected-resource` → the AS metadata → `userinfo`, once per sign-in, cached in the grant blob, with negative results held in memory so a restart retries. Two guards worth keeping: the token is only ever sent to the **issuer's own origin** (the metadata naming that endpoint was fetched unauthenticated, so a cross-origin `userinfo_endpoint` is exactly what must not be followed), and the probe never blocks `/api/servers`, which is polled every two seconds — it runs in the background and lands on the next poll, memoised against the access token so a JWT isn't re-decoded thirty times a minute. A provider that reveals nothing gets `👤 signed in`, not a guess. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Run on startup, on an install with no shell**: `apps/daemon/src/autostart.ts`. The Rust shell already did login items properly and the daemon delegated to it — but the shell is optional, and without it `startupSupported` was `false` and the UI said *"Autostart isn't wired up on win32 yet"* on Windows. The daemon now writes the same three mechanisms itself (`reg.exe` for HKCU Run, a LaunchAgent plist, an XDG entry), launching `hypergate tray` where the shell exists and `hypergated` where it doesn't — resolved from PATH, then the standalone executable, then `execPath + execArgv + argv[1]` for a checkout, so the item never points at something that isn't there. Settings names the mechanism, prints the command it will run, and surfaces a write failure instead of letting the toggle spring back. Verified live against the real registry: on → the Run value exists, off → it is gone. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **"server healthy", with the stop beside it**: the topbar pill answers what it is asked rather than naming the implementation, and reveals **Stop server** on hover (arm, then confirm) — the same hover-reveal rule the update check already uses, so the one control that ends the session isn't a stray click away. Always visible where there is no hover. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Double-click the tray icon to open the app**: on Windows the menu moves to right-click and `TrayIconEvent::DoubleClick` opens the manager, which is what a tray icon means on that platform (the tooltip says so). Unchanged elsewhere on purpose: the crate only synthesises `DoubleClick` on Windows, macOS menu bar extras open on a single click, and Linux SNI delivers no clicks at all — so the menu stays the surface that exists everywhere. **Start minimized** finally does something too: it was a stored preference nothing read, and now a login-time `tray` opens the manager when it is off. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **Closing hides, and asks the first time**: `DaemonSettings.closeAction` (`ask` → `tray` | `quit`). The window is hidden rather than destroyed, so reopening is instant and keeps its state. With no preference the shell calls `window.__hypergateAskClose()` and waits for wry IPC. The subtlety that only showed up under test: the 3s "the page never answered" deadline was firing **while the dialog was on screen**, yanking the window away mid-read — so the page now posts `close:asking` the moment the prompt renders, and that cancels the deadline. Quit-on-close stops the daemon even when the tray only attached to one; the menu's Quit still doesn't, because a daemon someone else started isn't ours to kill. Verified end to end by posting a real `WM_CLOSE` to the native window in all three modes: hide, prompt-stays-up-past-the-deadline, and quit-with-the-daemon-stopped. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **Verified**: 112 core + 25 daemon unit tests (new: the account/claims/discovery matrix, the connect catalog per client, and a daemon autostart suite that round-trips the real HKCU Run key on Windows and an XDG entry on Linux), 12 shell integration tests, `npm run build`, and `smoke:features` green. The UI was driven in the browser: the picker, an official agent's single-client panel, a manual client's endpoint + header, the custom agent's client chooser and inline rename, the account chip, the startup toggle against the live registry, and the close prompt. · Done: 2026-08-01

## Epic 16: the update that actually updates (v0.15.0)

Philip: *"let's improve the update feature. The check for updates button should replace the version text (same element). when checking, give a small inline spinner for at least 500ms. if there is an update available, replace with update available xxversion and the small buttons for download & install, download only, skip. give another spinner and progress bar when installing and start the install process."* Plus the ground truth behind it: *"it's currently on v13, and it didn't properly update to v14."*

**Why v0.13.0 never saw v0.14.0.** Two independent faults, neither of them in the update UI. (1) **Nothing was ever published** — `npm view hypergated` 404s and the repo had no tags or releases — so `update.json` on Philip's machine held `{"checkedAt":"…"}` with no `latest`, and `~/.hypergate/update.log` did not exist at all, i.e. the apply path was never reached. (2) **The release workflow could not cut a release**: it ran `npm publish` under `set -euo pipefail` *before* `gh release create`, and `NPM_TOKEN` was never configured, so tagging would have killed the job before the release existed. Both are fixed here; the second is why the fix has to include CI.

- [x] **The version element is the whole update surface** (`VersionBox` in `apps/web/src/App.tsx`): idle shows `v‹x›` (plus a quiet `· latest` once a check has confirmed it, because silence reads as "hasn't looked"); hover **replaces the label in that same element** with **Check for updates**; checking shows an inline spinner; an answer shows **↑ Update available v‹x›** with **Download & install** / **Download only** / **Skip**; a download shows a determinate bar with a real percentage; an install shows an indeterminate bar and *restarting*. The element's `min-width` is the width of the longest label, so swapping it never nudges the nav. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **The 500ms floor**, and why it is a floor and not a delay: `Promise.all([check, sleep(500)])`, so a slow daemon is never made slower and a cached answer — which returns in single-digit milliseconds — still reads as work rather than as a glitch. Measured in the browser at 504ms. · Done: 2026-08-01
- [x] **One hook, two surfaces** (`useUpdater`): the topbar and Settings → Updates render the same derived stage from the same state, so they cannot disagree about whether something is downloading. Caught a real bug the moment they shared it: the topbar was offering **Download & install** on a repo checkout, where Settings correctly refuses one-click. A channel with `canApply: false` now gets **How to update** instead, and `/api/update/download` refuses it too rather than fetching a package that could never be installed. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **The daemon downloads; the shell installs** (`apps/daemon/src/updater.ts`, ~9 daemon tests). The split is what makes "download only" real: fetching touches nothing the daemon is running from, so the daemon can do it while staying up and reporting bytes, whereas installing replaces its own files and must be done by something that outlives it. Payload lands in `~/.hypergate/updates/<version>/` behind a `manifest.json` written last, so an interrupted download can never look ready. Verified against the feed's `integrity` (sha512) or `shasum` before a file loses its `.part` suffix, size-checked, and only fetched over TLS or from loopback. New endpoints: `GET /api/update/progress`, `POST /api/update/download`, `GET /api/update/result`; `apply` now answers **202** and does the work behind the progress state. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **Updating with nothing on npm**: the payload is the same two npm packages either way, so `assetsFromGithub` picks `hypergated-‹v›.tgz` + `hypergate-shell-‹os›-‹arch›-‹v›.tgz` off the release **by exact name** (the release also carries installers, bare binaries and checksums; matching "something .tgz-ish" is how you install the wrong architecture), and the shell installs the staged files with `npm install -g <tgz> <tgz>` instead of naming a registry version. That is the whole reason an update can happen at all before a publish. · Done: 2026-08-01
- [x] **A determinate bar on both feeds**: a GitHub release states each asset's size, an npm packument does not, so unknown sizes are resolved with a HEAD before the download starts — two tiny requests against several megabytes. A feed that won't answer them leaves the bar indeterminate rather than inventing a percentage. · Done: 2026-08-01
- [x] **Saying how it went**: the updater writes `~/.hypergate/updates/last-result.json` before relaunching; the new version reports it once as **✓ Updated to v‹x›** and the daemon clears it on read. Failures are visible too — **Update failed** with Retry, and an install that never took the daemon down is called a failure after 60s instead of spinning forever (being alive to report an install is itself the evidence it never started). · Done: 2026-08-01
- [x] **Skip, per version**: `DaemonSettings.skippedUpdate`. It hides the offer from the topbar without hiding the truth (`updateAvailable` stays honest, Settings still shows it with **Un-skip**), and a forced check spends it, because pressing the button means you want to know. · Done: 2026-08-01
- [x] **CI can cut a release again** (`.github/workflows/release.yml`): the npm tarballs are packed **whether or not** a token exists and attached to the release (they are what the updater installs), and `npm publish` is now gated on `secrets.NPM_TOKEN != ''` with a note when it is skipped. A missing token must not take the job down and leave no release at all — which is exactly what it did. · Done: 2026-08-01
- [x] **Verified**: 108 core + 32 daemon unit tests (16 new: asset resolution from both feeds, the loopback/TLS rule, and a `Updater` suite running against a real HTTP server for integrity failure, truncation, join-in-flight, staging hygiene and the install deadline), 31 shell unit + 12 integration tests, `cargo fmt`/`clippy` clean, `smoke:http`/`smoke:features`/`smoke:shell` green. `smoke:update` was rewritten around real tarballs with real hashes and now proves both feeds end to end: the GitHub release path downloads its assets, and the npm path goes all the way through skip, a corrupted-tarball rejection, download-only, staging, guards, an accepted apply that stops the daemon, an install of the **staged** files with no second download, and the recorded result. In the browser, against a packaged npm-channel daemon and a throttled stub feed: hover swapped the label in-place at a fixed 112px, the spinner held 504ms, the bar climbed 10→26→37→55→73→81→100% and handed over to an indeterminate *Installing … restarting*, and the checkout channel showed **How to update** instead. Screenshots still time out on this machine, so the UI was read through the DOM. · Done: 2026-08-01

## Epic 17: what the first real update found (v0.15.1)

- [x] **An install ends when a different daemon answers, not when a request fails.** The v0.15.0 UI decided the install was over by watching `/api/update/progress` *fail* — reasonable, since the install stops the daemon reporting it. But the new daemon answers that endpoint perfectly well, reporting an idle job it knows nothing about, so a successful poll is not evidence the install is still running, and the page could sit on the old version indefinitely. Both paths now ask the same question instead: what version is serving, compared with the one that was serving when the page loaded. Only a *different* answer ends the wait. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **Verified**: the states re-walked in the browser against a throttled stub feed, including the one that was wrong — with the same daemon still answering, the topbar holds *Installing … restarting* for 3s+ rather than reloading out from under itself. · Done: 2026-08-01

## Epic 18: the daemon could never find the shell binary on a real npm install (v0.15.2)

Found by updating Philip's actual machine rather than a test layout. `apply` returned **"the hypergate shell binary is not available to run the update"**, and `/api/settings` reported `startupVia: daemon` where it should have said `shell`.

- [x] **The cause**: `locate()` in `apps/daemon/src/shell.ts` looked for `hypergate.exe` on `PATH`. A global npm install puts `hypergate.cmd` / `.ps1` / a shell script in the npm bin directory and the **real executable inside the per-platform optional dependency** (`node_modules/hypergate-shell-win32-x64/bin/hypergate.exe`), which is not on `PATH` and never will be. So the scan found nothing on the one channel one-click updates are offered on. The daemon now resolves the platform package with `createRequire(...).resolve()`, exactly as the CLI shim has always done (`packaging/npm/bin/hypergate.mjs`), and the shell additionally passes `HYPERGATE_SHELL_BIN` when it spawns the daemon, so a layout neither of them anticipates still works. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Done: 2026-08-01
- [x] **The blast radius was wider than updates.** Everything behind `run()` degrades silently when the binary is missing, so on every real npm install: **OAuth grants and the gateway token were being written to `~/.hypergate/oauth/*.json` instead of the OS keychain** (verified on Philip's machine: a 646-byte `context7.json` on disk while `hypergate secret check` answered `available`), and **Run on startup** fell back to the daemon-written login item instead of the shell's. Both are restored by the same fix. · Done: 2026-08-01
- [x] **Why three releases of tests missed it**: `smoke-update.mjs` set `HYPERGATE_SHELL_BIN` for convenience, with a comment explaining that the test layout has no PATH entry — which is precisely the condition a real install is in. The override is now gone, and `startupVia === 'shell'` is asserted directly, so the daemon must find the binary the way a user's install requires. A test that hands the code an answer the product never gets is not testing the product. · Done: 2026-08-01
- [x] **Verified**: `smoke:update` green with no override; on the real machine the in-app update ran end to end (v0.15.0 → v0.15.1 off the GitHub release: staged both tarballs, installed them with no second download, relaunched, cleaned up, and came back reporting `v0.15.1 · latest`). · Done: 2026-08-01

## Epic 19: a window, not a page (v0.16.0)

Philip: *"let's shift the layout a bit of hypergate. I want to change where the the app container isn't scrollable. When the window is resized, the layout and content can adjust the lists internally can be scrolled"*

The manager was a web page in an app frame: `body` grew with its content and the whole thing scrolled, so a long server list pushed the footer past the bottom of the window and resizing changed how much of the page you could see rather than how much of each list.

- [x] **The shell** (`.app` / `.wrap` / `.view` in `apps/web/src/styles.css`, `App.tsx`): `html, body, #root` are full-height and `body` no longer scrolls. The topbar and the footer are fixed rails (the topbar stopped being `position: sticky`, because nothing scrolls under it any more), and the view between them takes the remaining height. Each view's own children are flex items: chrome (`pagehead`, the gateway bar, section titles) is `flex: none`, panels are `flex: 0 1 auto` with the list inside them scrolling. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Shrink in proportion to what a panel has to show**, which is what plain flexbox does with a content-derived basis: given a servers list of 2250px and an agents list of 158px, the deficit lands almost entirely on the servers list. Floors stop a panel disappearing: 64px normally, 96px once a list is worth scrolling (`:has(.list-row:nth-child(3))`, so a one-row Settings panel is never padded out to a floor it doesn't need). Floors are deliberately mean: an over-generous floor overflows the view, and once the view overflows *every* panel sits at its floor instead of sharing the height out. A 132px floor cost the servers list 100px of the space it should have had; 96px gave it back. · Done: 2026-08-01
- [x] **What doesn't shrink**: an empty state is a pitch and a button rather than a list, so it keeps its natural height and lets the view scroll instead (first run has little else on screen). An on-demand panel (add/edit form, the agent picker) floors at 220px, because it is only on screen at all because it was asked for, and it scrolls itself into view on mount (`useRevealOnMount`) so a button press never opens something below the fold. · Done: 2026-08-01
- [x] **Analytics had more furniture than height**: the intro, the callout, the metric bar and the sparkline are 400px of fixed chrome above three lists. **Usage by server** and **Who's calling** now sit side by side (`.duo`) while the window is wide enough, and stack below ~860px (where the pair dissolves with `display: contents` so its sections shrink like any others). The callout moved into the empty state. That is the difference between the whole view fitting a 1200x820 window and it scrolling. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **A short window spends its height on lists**: under 660px the page intro's paragraph, the analytics callout and the gateway bar's footnote are hidden and the section spacing tightens, which is worth ~120px at the 430px minimum the window can be dragged to. · Done: 2026-08-01
- [x] **Verified**: `npm run build`, 108 core + 32 daemon tests, `smoke:http` green. In the browser at 1440x1000, 1200x820, 820x700, 620x430 (the shell's minimum inner size) across all three views, measured through the DOM: `document` never scrolls in either axis, the topbar stays at y=0 and the footer at the window's bottom edge, no panel overlaps its neighbour, and every over-long list reports `scrollHeight > clientHeight` on its own scroller. Also walked the interactions that change the panel count: opening the CLI section, the catalog, the custom-server form and the agent picker (each revealed itself in the view), expanding a 60-tool inspector inside the servers list, and a stubbed first-run state where both empty states keep their full height. Screenshots still time out on this machine (the pane doesn't composite), which is also why the reveal is a plain `scrollIntoView` rather than a smooth one. · Done: 2026-08-01

## Epic 20: the desktop app's dead links, and three things it said wrong (v0.16.1)

Four reports off one screenshot of the running app: sign-in did nothing, the layout work wasn't there, Disconnect and Remove looked like the same button, and the CLI list called ten tools "missing".

- [x] **Links out of the app went nowhere** (`apps/shell/src/window.rs`): the webview had no new-window handler. WebView2 raises `NewWindowRequested` for `window.open` *and* `target="_blank"`, and with nobody listening it drops the request on the floor — no window, no navigation, no console error. So "Sign in" on a remote server, every catalog **docs** link, the release-notes link and the footer GitHub mark all did nothing in the desktop window while working in a browser tab. Web URLs now go to the user's real browser and the in-app popup is denied, which is the better destination anyway: a chromeless frame with no address bar is the wrong place to type a provider password, and the browser is where they are already signed in. Only `http(s)` is passed on — `open` hands anything else to the shell that launches programs, and for a remote MCP server the sign-in URL comes from a page we did not write. · [spec](SPEC.md#33-registry--catalog-shipped) · Done: 2026-08-01
- [x] **The sign-in popup asks over IPC** (`open:<url>`, alongside the existing `close:*` verbs) rather than trusting `window.open` to survive the trip. It does survive on WebView2 — measured, below — but the OAuth call goes through an `await` before it opens anything, and a popup blocked for want of a user gesture never becomes a new-window request at all. · Done: 2026-08-01
- [x] **The window opens wider**: 1360x900 rather than 1200x820, shrunk to 90% of the primary monitor when the screen is smaller. `with_inner_size` is a request, not a constraint, so a fixed default sized for a desktop opens off the bottom of a laptop. · Done: 2026-08-01
- [x] **Remove means removed** (`apps/daemon/src/index.ts`, `apps/web/src/App.tsx`): the row carried both **Disconnect** and a trash button, and the destructive-sounding one destroyed *less*. Disconnect dropped the OAuth grant; Remove deleted the config row and left the grant in the keychain forever, so re-adding the same server came back already signed in as whoever set it up last, and `secretDelete` was dead code the repo never called. One control now: Remove stops the server, `deleteOAuth` drops the whole keychain entry (and the legacy file, and the in-memory cache), the config row goes, and the id is pruned from every agent's allow-list (`'*'` needs no pruning — it means "every server there is", and this one no longer is one). `POST /api/servers/:id/disconnect` and `api.disconnect` are gone. Because Remove now takes the sign-in with it, the trash arms first: a second click on **Remove & sign out**. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **"Missing" was a fault report for software nobody asked for** (`CliSection`): the section listed all 22 known CLIs and put a `missing` pill on the ten you don't have, which read as *something is wrong* when it meant *we have heard of this and you have not installed it*. It now lists what you have ("12 detected"), and the rest sit behind **Install a tool** as an offer. Each gets the control its install actually needs, because the catalog's `install` field is honestly three different things: a copyable command (`npm i -g wrangler`), a **Get it ↗** link (a URL), or the plain sentence when it ships with something else ("Comes with Node.js"). · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **The gate, turning** (`GateMark`, `styles.css`): the topbar mark was the favicon in an `<img>` on a gradient tile — a dark tile inside a gradient square, two backgrounds arguing. It is now the hero shader's ring rebuilt for 28px: a conic sweep violet → cyan → ice behind an annular mask, rotating once every 7s, around an event horizon that breathes. Conic rather than a stroked SVG circle, because at this size what reads is the colour travelling round the ring, not a shape spinning. The favicon (web + site, byte-identical) is the same mark held still, plus the halo the ring throws onto the space around it; the entry streaks are gone, since they read as scratches at 16px. Both animations stop under `prefers-reduced-motion`. · [spec](SPEC.md#4-design--brand) · Done: 2026-08-01
- [x] **A malformed account blob took down the whole page**: `AccountChip` called `a.source.replace(…)` on a value parsed out of a keychain entry, so a grant written by an older build white-screened the app rather than degrading one chip. Found by seeding a fake grant during verification. Guarded. · Done: 2026-08-01
- [x] **Verified.** Links: drove the real WebView2 window over CDP against an isolated daemon (port 7801, own `HYPERGATE_DIR` and keychain namespace) with a log server recording which URLs reached a browser — the IPC path, a real click on a `target="_blank"` anchor and `window.open(…,'noopener')` all arrived; `file:///C:/Windows/System32/calc.exe` did not, and no calculator started. Measured inner size 1360x900. Remove: seeded a grant that lived in *both* the keychain and the legacy file plus two agents (one scoped to the server, one `'*'`), removed it through the UI, and confirmed the config row, the keychain entry and the file were all gone and the scoped agent's list pruned to `["ctx7"]` while `'*'` was untouched. CLIs: 12 rows, zero `missing` pills, "10 more tools" behind the button, and all three install shapes rendering. Mark: 28x28, conic ring masked to an annulus, `gate-spin`/7s and `gate-breathe`/3.4s live. 45 Rust + 140 JS tests green. · Done: 2026-08-01

## Epic 21: server-first dashboard hierarchy (next)

Philip: move the gateway URL and token controls into the title row, keep the MCP server list as the page's primary focus, connect the add-agent picker to the existing agent rows, and preserve internal app scrolling without browser-style document scrolling.

- [x] **Compact gateway controls in the page header**: the URL, revealable master token, and copy actions sit beside the title on wide windows and stack below it when width is constrained. The detached gateway card and its redundant pointer to Connected agents are gone. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Add agent as part of the roster**: the picker renders inside the connected-agent panel as its next row when agents already exist, preserving quick-connect behavior, reveal-on-open, and the responsive card grid. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Window-native overflow verification**: the production build and 140 JS tests pass. Rendered checks at 1360x900 and 620x700 show the gateway beside and below the title respectively. CDP confirms zero document overflow in both axes, `body` and `.app` remain `overflow: hidden`, `.view` owns fallback scrolling, and the opened 11-target agent picker is the final row inside the agent panel with its list scrolling internally. The layout detector reports no findings. · Done: 2026-08-01
- [x] **Recognizable bundled agent marks**: Claude, Cursor, Gemini, and VS Code use their native vector marks; the remaining clients use distinct local product marks. The same marks replace generic dots in connected-agent rows and per-server agent permissions, including name-based fallback for pre-target legacy agents. No runtime icon request or dependency was added. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Replace the generic card grid**: the picker is now a responsive three, two, or one-column launchpad of open, icon-led rows. Hairline separators replace nested cards; connected, ready, CLI missing, config, manual, and loading states are explicit; the next-action chevron moves on hover and keyboard focus. Legacy agents match by product name so Connected is truthful before their stored target is upgraded. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Agent chooser verification**: full build and 140 JS tests pass. Rendered at 1360x900 and 620x700 with 11 choices, zero document overflow, internal picker scrolling, three desktop columns and one narrow column. CDP confirmed one Close action, visible inset keyboard focus, Connected states for legacy Claude Code and Cursor agents, and the corresponding native marks in their connected rows. · Done: 2026-08-01

## Epic 22: a row you can open (v0.17.0)

Philip: make servers and agents expandable when you click on them; drop the Agents and Logs buttons on a server row and make them text, with the opened row showing all of it (log window, connected agents, …); put the last log line in the collapsed row; move restart and the switch next to remove; unbox the restart/remove buttons and enlarge the icons instead.

- [x] **The row is the disclosure** (`ExpandRow`, `apps/web/src/App.tsx`): one component behind both lists. The head is the click target (not a wrapping `<button>`: it holds switches, an editable name and links, and interactive content inside a button is neither valid nor navigable), the caret is a real `aria-expanded` / `aria-controls` button for the keyboard, and the actions group stops propagation so a click on a control is about the thing, not about the row. The opened half scrolls itself into view. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Three buttons that asked one question**: the server row's `n tools` link, `Agents n/m` disclosure and `Logs` toggle all meant "tell me more about this row". They are now text (`3 tools · 1/2 agents`, amber when no agent may use the server) and the row opens onto **Tools**, **Agents** and **Logs** as labelled sections. The tool list is capped at 264px and the log pane at 200px, since a 60-tool server would otherwise make its row taller than the window. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **The last line, in the collapsed row** (`ServerStatus.lastLog`): carried on the status the UI already polls rather than a request per row: `toStatus` reads the supervisor's ring, and the daemon fills it from the durable rows (`LIMIT 1`) for a server whose ring is empty after a restart. Suppressed on an errored row, where the error is the same news in a sharper voice, and absent for a remote server, which has no local output to have. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **A log window, not a log dump**: an open row tails its server every 3s and follows the newest line the way a terminal does, but only while you are already at the bottom, because scrolling up to read something is a decision and a pane that yanks you back two seconds later is unusable. · Done: 2026-08-01
- [x] **Unboxed actions** (`.btn-icon`): switch, restart and remove sit together at the right edge with nothing between them. Restart and remove lost their borders and grew their glyphs 16px → 18px on a 30px target; they rest at `--ink-3`, come up to `--ink-2` when the pointer is anywhere on the row, and take their semantic colour (amber, red) only on hover. Remove still arms before it fires. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Agent rows open too**: the collapsed row states what the agent may reach (`all servers`, `can use 1/4 servers`, or a red `blocked, no servers`) and when it was last used; the token, the per-server chips and the Connect panel moved inside. The **Connect** button is gone (the row opening onto the panel is the same click) and the picker's quick-connect still forces the row open from outside, with a close that clears that intent so it cannot spring back. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Verified.** Build green across all four workspaces; 108 core + 32 daemon tests pass. Against a throwaway daemon (port 7799, own `HYPERGATE_DIR`, keychain namespace `ui-verify`) seeded with a ready server, a *chatty* one writing a heartbeat to stderr every 2.5s, an errored one and an `authorizing` remote: `/api/servers` carries `lastLog` for the two that have said something and omits it for the two that have not. In the page (CDP-driven, since screenshots still time out on this machine) a click on the row body opens it, `aria-expanded` and the caret label flip, the panel holds `Tools 1 / Agents 2/2 / Logs`, the row tints to `--card-2`, a click inside the actions group leaves it open, the caret closes it, the focus ring lands on the caret, and the icon buttons measure 30px with a transparent border and an 18px glyph. Captured collapsed, server-open, agent-open and light-theme frames. · Done: 2026-08-01

## Epic 23: downloads first and release notes (v0.17.0)

Philip: make the matching device download the marketing site and README's first CTA, keep GitHub second, show the dynamically resolved latest version in the site button, and add release notes to the site.

- [x] **Release-aware download resolver**: the marketing site fetches the latest published GitHub release, detects desktop OS and architecture, matches its installer, and updates the first CTA with both the release version and direct asset URL. Linux defaults to DEB with a caret menu for RPM and tarball; macOS adds an Apple silicon / Intel override because Safari conceals architecture; mobile omits the unavailable action and promotes GitHub; API failure falls back truthfully to the releases page. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-01
- [x] **Release notes page**: added an indexed, responsive page in the existing deep-space visual system that renders published GitHub release names, dates, safely formatted notes, and matching downloads, with accessible loading, empty, and error states. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-01
- [x] **Download-first README and stable assets**: the README now leads with direct platform downloads and names the repository next. Release automation retains versioned assets while also publishing stable latest-release aliases. The five aliases used by the README were added to the existing v0.16.1 release and verified live; the Intel Mac link uses the existing exact-name standalone asset until the corrected cross-build emits an Intel package in the next release. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-01
- [x] **Verification**: full repository build passed; 108 core and 32 daemon tests passed with 2 platform skips; site TypeScript check and multi-page production build passed; all six README links returned HTTP 200; desktop release notes and mobile CTA behavior were rendered in Edge. The Impeccable detector reported only the site's incumbent Space Grotesk / Inter font choice, which remains the documented design system rather than being changed in this scoped extension. · Done: 2026-08-01

## Epic 24: navigation and release polish (v0.17.0)

Philip: move Connected agents before MCP servers, replace the top tabs with a clean left rail, add a scroll-aware nested navigation for Connected agents, MCP servers, and CLI tools, use smooth section snapping and taller natural lists without a visible main scrollbar, remove the marketing hero's rectangular scrim seams, show an OS-specific install-and-launch command, enable auto-merge, and ship the release.

- [x] **Left navigation and task order**: Servers, Analytics, and Settings now live in a responsive icon-led left rail. The expanded rail nests the three Servers sections, Connected agents comes first, and the compact 68px rail keeps keyboard-labelled top-level icons at narrow widths. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **One scroll surface with section awareness**: the main view now owns natural-height scrolling with a visually hidden scrollbar, smooth reduced-motion-aware movement, and proximity snap targets. The nested navigation tracks a reading marker as sections move past it; only bounded tool and log details keep their own scroll. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Quiet, spacious presentation**: the boxed top tabs and dashboard panel borders are gone. Sections get viewport-scale whitespace, agent-picker separators disappear inside the dashboard, and natural lists no longer compete for several tiny flex allocations. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-01
- [x] **Hero seam fix**: the finite radial scrim uses a closest-side fade that reaches full transparency at its expanded bounds, and the hero clips its canvas composition at the viewport. Rendered animation shows no rectangular edge and no horizontal overflow. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-01
- [x] **Warp-ring shader seam fix**: the liquid noise domain now uses continuous circular coordinates instead of raw `atan` values, removing the horizontal branch cut where the angle wrapped from +π to -π while preserving the ring's scroll-reactive motion. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-02
- [x] **Detected install-and-launch command**: the hero now shows a copyable one-line PowerShell, macOS Terminal, or Debian or Ubuntu Bash command built from the detected architecture and stable latest-release aliases. It downloads and installs the native package, then launches the binary without relying on a PATH refresh. Four pure command-generation tests cover all supported and unsupported device branches. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-01
- [x] **Verify, auto-merge, and release**: PR #41 merged at `cef7bc6`; repository auto-merge is enabled and `main` now requires all seven CI jobs with strict up-to-date checks and resolved conversations. Main CI passed. Tag v0.17.0 published 35 GitHub assets after a transient upload failure succeeded on retry, and all six stable installer aliases return HTTP 200. Production `hypergate.app` still serves the prior site because its saved Vercel token is invalid; Philip chose to leave the merged site source for a later authenticated deployment. · Done: 2026-08-01

## Epic 25: ambient blue-purple surface redesign (planned)

- [ ] **Translate the supplied visual reference into Hypergate's full manager UI**: replace the current mid-slate and bordered-card treatment with a near-black canvas, large softly rounded charcoal surfaces, generous whitespace, minimal separators, and controlled ambient blue-purple illumination around active or important controls. Do not copy the reference's rainbow treatment. Preserve Hypergate's existing information architecture and behavior while defining reusable surface, radius, spacing, type, glow, focus, and motion tokens; apply them consistently to navigation, gateway controls, agent and server rows, expanded details, catalog, analytics, settings, dialogs, loading, empty, error, hover, focus, reduced-motion, and compact-window states. Avoid a box around every item, keep text contrast accessible, and prove the system at representative wide, short, and narrow desktop sizes before shipping. Consolidate theme selection from Light, Medium, and Dark to **Light, Dark, and Auto**: Auto is the first-run default, resolves the live system preference before paint, updates when that preference changes, falls back to Dark when detection is unavailable, and migrates a saved legacy Medium value to Auto without disturbing explicit Light or Dark choices. · [spec](SPEC.md#34-ui-shipped) · [feature brief](../obsurdian/projects/hypergate/features/ambient-blue-purple-redesign/prompt.md) · Added: 2026-08-01

## Epic 26: update notice polish (next)

- [x] **Repair the available and staged update states in the topbar**: the status text no longer wraps into a circular badge. Status, version, and actions now form one compact action rail with aligned 28px controls, a clearly dominant install action, and quiet secondary actions. At 900px the redundant status phrase recedes, at 760px **Download only** moves to the full Settings surface, and at 640px **Skip** does too, keeping the primary action visible without header or document overflow. Rendered the available state at 1024×768 and 620×700, plus the staged state at 1024×700; all held `white-space: nowrap`, zero horizontal overflow, and the first Tab landed on the install action with a visible 3px focus ring. Full build and 143 JavaScript tests passed. The detector reported only the incumbent Inter font choice, outside this scoped repair. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-02 · Done: 2026-08-02

## Epic 27: agent status and site polish (next)

- [x] **Specify the refinement before implementation**: agent status uses recent authenticated calls rather than pretending the stateless gateway can observe a persistent socket. Active is at most one minute old, Online is at most fifteen minutes old, and Offline is older or never used. Connection instructions become a secondary Connect or Reconnect disclosure; token and permissions remain the opened row's primary work. Manual scrolling loses snap behavior while section-nav clicks keep reduced-motion-aware smooth movement. The product mark becomes a hollow portal across web and native surfaces. Mobile hides desktop-only install controls, and the already-merged download and release-notes site must be verified live. · [spec](SPEC.md#34-ui-shipped) · [feature brief](../obsurdian/projects/hypergate/features/agent-status-and-site-polish/prompt.md) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Refine connected-agent hierarchy**: improved identity, status, reach, usage, and action spacing at wide and narrow sizes; added labeled LED states with accessible descriptions; token and permissions stay visible when the row opens; client setup now sits behind a nested disclosure that says Connect before observed use and Reconnect afterward, while quick-add opens it automatically. Wide and 620px renders have zero horizontal overflow. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Remove section snap without removing smooth navigation**: removed every scroll-snap behavior and alignment target from the Servers view while preserving reduced-motion-aware `scrollIntoView` navigation. CDP held a manual 260px position without snapping, then a nav click moved MCP servers to its 18px scroll margin and updated the active item. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Unify the hollow warp-gate mark**: removed the glowing center from the manager mark, both site navigation marks, both favicons, and the procedural tray and window icon while preserving the violet-to-cyan ring, halo, compact-size clarity, and reduced-motion behavior. The native violet now matches the marketing token; nine pixel and format tests enforce the transparent center across RGBA, ICO, scaled, template, and SVG outputs. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Publish the verified marketing updates**: Windows gets a stable direct installer before GitHub metadata resolves and keeps that fallback on API failure; release notes are a multi-page production entry; mobile marks itself synchronously and hides both desktop download groups and the CLI install block. The first native Git deployment completed from `main` at Vercel deployment `AFyTVVzSrinJgHhxoh5fPHWqKYxw`. Live Edge renders resolve the v0.17.0 Windows x64 installer, hide the mobile CLI block and download groups, load four releases, and have zero horizontal overflow. · [spec](SPEC.md#37-marketing-site-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Verify the local pass**: 111 core tests, 32 daemon tests with 2 platform skips, 33 Rust unit tests, 12 Rust integration tests, 5 site tests, root and site builds, Rust clippy, diff checks, wide and narrow manager renders, desktop and mobile marketing renders, and the Impeccable detector completed. The detector only reports the incumbent fonts and pre-existing toast accent border. Live deployment remains the separate open item above. · Added: 2026-08-02 · Done: 2026-08-02

## Epic 28: OAuth branding identity cleanup (next)

- [x] **Specify provider-facing identity and asset ownership**: dynamic registration must send Hypergate's product page, stable software identifier, and one canonical hollow-gate logo URL. The local OAuth result and remaining product imagery use the same open portal. Provider review remains an external policy, so compliant metadata does not claim it can override a provider's generic unapproved-client treatment. · [spec](SPEC.md#33-registry--catalog-shipped) · [feature brief](../obsurdian/projects/hypergate/features/vercel-oauth-and-catalog-action/prompt.md) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Add OAuth client presentation metadata**: the shared provider now owns Hypergate's canonical `client_name`, `client_uri`, cache-busted `logo_uri`, and `software_id`; the daemon passes that identity into every dynamic registration, while the local callback loads the matching packaged favicon without depending on the public site. A focused unit test locks the exact metadata. Static pre-registered providers still control branding in their own provider console. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Remove remaining legacy product imagery**: replaced the paw in the callback and close-choice dialog, removed the centered event horizon from README and social imagery, and regenerated both raster derivatives from the corrected source. Source search no longer finds the paw, old violet, centered core, or orphaned modal style. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Verify the complete branding pass**: 111 core tests, 32 daemon tests with 2 platform skips, 5 site tests, root and site builds, diff checks, stale-source searches, regenerated image inspection, and an isolated rendered callback all pass. The callback loaded its packaged hollow favicon at 150px intrinsic width. The Impeccable detector reports only the incumbent font and pre-existing toast accent border. The live Vercel server is ready, signed in, and exposes 33 tools. Vercel's reviewed-client policy remains separate from code-controlled RFC 7591 metadata. · Added: 2026-08-02 · Done: 2026-08-02

## Epic 29: a connection that survives its own credential (v0.18.0)

Philip, trying to add Hypergate to Claude Code: *"it's having troubles. We're missing https, I don't see where to add the id and token. It would be nice to have an easy one click button option… how do we register with Claude?"* Three separate things behind that. The **custom connector** dialog he was in can never reach a local daemon (Anthropic's cloud opens the connection, hence https and a public host), so that door is closed by design and not worth chasing. The Claude Code entry he already had was failing for an unrelated reason: it held a **stale token**, because connecting copies a credential into a client config and nothing keeps the two in step. And "registering with Claude" is real, but through two doors this repo had neither of: a **plugin** and an **MCPB bundle**.

- [ ] **`hypergate mcp-headers <agent> [--create]`** — the credential a client fetches instead of storing. Prints `{"Authorization":"Bearer …"}` and nothing else on stdout; errors go to stderr with a non-zero exit, because the caller parses stdout as JSON. Claude Code runs it at every connection and re-runs it on a `401`, so a rotated token, a re-created agent or a moved port costs a reconnect instead of a support question. `--create` makes the agent on first use (scoped like the quick-connect button's), and says so on stderr, since minting a credential silently is the wrong amount of quiet. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-02
- [ ] **Agent resolution that outlives an id** (`core/agents.ts`) — an agent id is `${slug(name)}-${4 hex}`, so a config written against `claude-code-a8ce` is worthless the moment that agent is deleted and re-created. `matchAgents` resolves a key in three tiers — exact id, display name, then id stem with the minted suffix off *both* sides — and returns every candidate rather than picking one, because the answer hands out a credential. The daemon exposes it as `POST /api/clients/resolve` and mints ids with the same `agentSlug`, so there is one implementation of what an agent is called. · Added: 2026-08-02
- [ ] **The connect button writes a helper, not a token** — `ConnectContext` grew an optional `headersHelper`, and Claude Code's argv becomes `claude mcp add-json` (there is no `--header`-style flag for it). Only after the daemon has *run* `hypergate mcp-headers` on this machine and seen real headers come back: an older `hypergate` on `PATH` has no such subcommand, and a config pointing at a helper that isn't there is strictly worse than the token it replaced. Every other client keeps the bearer token, `.mcp.json` included — it is read by half a dozen harnesses that would choke on a field only Claude Code knows. · [spec](SPEC.md#32-gateway-shipped) · Added: 2026-08-02
- [ ] **`--stdio` proxies to the resident daemon** (`core/proxy.ts`) — it used to call `startEnabled()` unconditionally, so every harness that spawned `hypergated --stdio` got its own private copy of every managed server: three open editors, three Postgres connections, three sets of logs. It now probes `/health` first and, when a daemon answers, forwards `tools/list`/`tools/call` to its `/mcp` instead. Namespacing, permissions and usage recording stay where the servers actually live. With no daemon it behaves exactly as before, and `HYPERGATE_STDIO_PROXY=0` forces that. `HYPERGATE_STDIO_AGENT=<key>` makes the spawn present a scoped agent token rather than the master one, which is what the bundle uses. · [spec](SPEC.md#32-gateway-shipped) · Added: 2026-08-02
- [ ] **Claude Code plugin + `nekko-labs` marketplace** — `/plugin marketplace add nekko-labs/hypergate` then `/plugin install hypergate@nekko-labs`, and the gateway is there in every project with no token, no port and no JSON to paste: the plugin's `.mcp.json` names the helper command and reads `${HYPERGATE_PORT:-7777}`. This is the "one click" ask, and the door to Anthropic's plugin directory. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-02
- [ ] **MCPB bundle for Claude Desktop** (`npm run build:mcpb`) — the same daemon bundle the npm package ships, packed by the official `@anthropic-ai/mcpb` packer into a double-clickable `hypergate.mcpb`. It runs `--stdio`, so it shares the machine's fleet when a daemon is up and runs the servers itself when not, and it announces itself as a `claude-desktop` agent so Desktop is revocable on its own in the manager. Desktop is the surface where a *button* exists at all — Claude Code has a CLI, Desktop has an install dialog. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-02
- [ ] **PRIVACY.md** — required for a directory listing (a missing privacy policy is an immediate rejection) and honest to write for a product with no backend: what is stored, where, for how long, and the four user-initiated network calls. Referenced from the MCPB manifest's `privacy_policies`. · Added: 2026-08-02
- [ ] **Directory submissions need Philip** — the Claude Code plugin goes through Anthropic's plugin submission, and the `.mcpb` through the desktop-extension form; both are human steps with an account attached. The remote-MCP **connector** portal is *not* a door for us: it takes internet-hosted `https://` servers only, which a local-first gateway is not. Also outstanding: signing the bundle (`mcpb sign`) once the release signing story lands, and an icon for the listing. · Added: 2026-08-02

## Epic 30: automatic marketing deployment (next)

- [x] **Connect the existing Vercel project to GitHub**: linked Vercel project `hypergate` to `nekko-labs/hypergate` through the already-installed organization GitHub App, selected `main` as the production branch, and set the monorepo root directory to `apps/site`. Framework remains Vite with automatic install/build commands and `dist` output. This is the native Git path, not a duplicate project or a token-bearing GitHub Actions workflow. · [spec](SPEC.md#37-marketing-site-shipped) · Added: 2026-08-02 · Done: 2026-08-02
- [x] **Prove the first merge-driven production deployment**: pushed the reconciled `main` revision `e3c7500`; Vercel status completed successfully at deployment `AFyTVVzSrinJgHhxoh5fPHWqKYxw`. Live verification confirmed the direct v0.17.0 Windows x64 installer, four rendered release entries at `/release-notes.html`, mobile with both desktop download groups and the CLI snippet hidden, and zero horizontal overflow across all three renders. · [spec](SPEC.md#37-marketing-site-shipped) · Added: 2026-08-02 · Done: 2026-08-02

## Epic 31: Kotrain becomes Nekkos (reference rename)

- [x] **Rename every Kotrain / Open Paw reference to Nekkos**: the sibling product (repo `nekko-labs/kotrain`, formerly `open-paw`) is now **Nekkos** (repo `nekko-labs/nekkos`, home nekkos.app, CLI `nekkos` with a legacy `kotrain` alias; `opaw` dropped). Updated the catalog entry, CLI detection, connect target (`~/.nekkos/settings.json`), agent brand mark, docs (README, SPEC, TASKS, llms.txt), site copy, shell/daemon comments and tests, and smoke scripts. Nekko Labs, nekko-labs, and NekkoMCP history stay as they were. · Done: 2026-08-03

## Epic 32: Nekkos becomes Kotrain again (reference rename, reversed)

- [x] **Reverse Epic 31: every Nekkos reference goes back to Kotrain**: the rename above lasted a day. The sibling product is **Kotrain** again (repo `nekko-labs/kotrain`, home **kotrain.com**, CLI `kotrain` with a legacy `nekkos` alias), so the catalog entry id, CLI detection, connect target (`~/.kotrain/settings.json`), gateway tool prefix (`kotrain__kotrain_*`), agent brand mark, docs (README, SPEC, TASKS, llms.txt), site copy, shell/daemon comments, tests and smoke scripts all follow it back. Epic 31's entry above is left as written, since it is the record of what happened. Nekko Labs, nekko-labs, NekkoMCP history, and the Nekko Vault catalog entry stay as they were. · Done: 2026-08-03

## Epic 33: a hero that names the fear, and a version rule (v0.18.0)

Philip: *"add the rule, each PR should bump the release version by a minor version unless mentioned"*, plus the marketing hero: `"One gate"` → `"One secure gate"`, and a subhead rewritten around safely connecting agents to real services.

- [x] **The hero asks the visitor's question instead of describing the machine**: `One gate.` → **`One secure gate.`**, and the subhead moved from *"runs your MCP servers on your own machine: sandboxed, supervised, and aggregated behind a single gateway endpoint"* to the thing a visitor is actually weighing: connecting Claude Code, Codex, Kotrain, Cursor or Devin to **GitHub, Vercel, Slack or Google Drive**, easily and safely, with authentication, keys and permissions kept out of an agent's hands *if things go bad*. Named services because a visitor recognises the service, not the protocol; the loss framing because that is the moment the product is worth installing. The mechanism words were not deleted, they already live in the security section, the isolation cards and the quick start. Verified at 1440×980, 1024×768, 820×900, 390×844 and 360×780: the longer gradient line still sets on one line at every width where the old one did (803px inside an 880px measure at full size), the subhead grows from three lines to five on desktop and seven on mobile while the hero still ends 152px above the fold, and no width scrolls horizontally. · [spec](SPEC.md#37-marketing-site-shipped) · Added: 2026-08-04 · Done: 2026-08-04
- [x] **Every PR bumps the minor** (§4.1 above, `scripts/bump-version.mjs`, `npm run version:bump` / `version:check`): the version is written in **seven** files (root and daemon `package.json`, the daemon's `VERSION` constant, `Cargo.toml` + `Cargo.lock`, the plugin manifest and the marketplace entry), so a rule enforced by hand is a rule that drifts. The script moves all seven, `--check` fails on disagreement, and `--patch` / an exact version cover the "unless mentioned" case. The rule matters because releases are cut by tag and the publish job compares the tag to the root `package.json`: a merged PR that left the version alone cannot be released on its own. Recorded in `TASKS.md` §4.1, the README's Develop section, and the project's workspace `AGENTS.md`. This PR is the first to follow it: 0.17.0 → **0.18.0**. · Added: 2026-08-04 · Done: 2026-08-04

## Epic 34: release notes a person can read (v0.19.0)

Philip: *"let's clean up the release notes. they are too technical. make them more human readable. Identify what changed grouped by features / bug fixes / performance improvements."*

- [x] **What changed, in the reader's words**: every release's notes are `docs/releases/<version>.md` — a headline, one line on what the release is for, then **Features**, **Bug fixes** and **Performance improvements**, with a group left out when it would be empty rather than padded. All four published releases (v0.15.1 through v0.17.0) backfilled from the commits they shipped, because before this every one of them said only how to install it and nothing about what it changed. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-08-04
- [x] **A generated install footer**: `scripts/release-notes.mjs` appends it, so it reads the same on every release and nobody writes it by hand, and it keeps the one thing that has to vary — the registry command appears only when that release published, and v0.15.2's notes had been promising `npm install -g hypergated` while nothing is on the registry at all. The release workflow and the by-hand backfill both go through the script, and the workflow's re-run path now sets the notes it used to skip. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-08-04
- [x] **A release's notes cover everything since the last released tag**: a minor bump per PR outruns the tags — v0.16.0 was bumped and never released, so its changes went out under v0.16.1, whose notes fold them in and say so. `docs/releases/README.md` names the rule and the `git log $(git describe --tags --abbrev=0)..HEAD` read-through that checks it, because the workflow can warn that a notes file is *missing* and never that it is incomplete. `docs/releases/0.19.0.md` follows its own rule: it covers everything merged since the v0.17.0 tag, 22 commits across the connect-credential work, the plugin and the bundle, the theme button, the agent logos, the remote bearer-token fix and the secure-gate hero, not just the release-notes change that bumped the version. Caught by the Devin review on PR #54, which was right that a rule the PR itself broke is decoration. · Done: 2026-08-04
- [x] **The site renders the shape**: `###` headings and `**bold**` become real DOM nodes (h3, since the release title is the h2 above it), and a bullet wrapped over several lines is one item instead of one item per line. The subset stays deliberately small and is named in `docs/releases/README.md`, because anything outside it shows up as its own punctuation on the page. · [spec](SPEC.md#37-marketing-site-shipped) · Done: 2026-08-04

## Epic 38: one bar, not two (v0.23.0)

Philip: *"remove the hypergate in top bar and just use the larger one and move it up to the top left corner. move the update pill, theme picker and server status up to the very top right after the standard window controls (same concept for mac and linux). move the github icon further to the right and the 'local first..' section over to the left."*

- [x] **The page's top bar is the window's title bar**: the manager window drops its OS frame (`with_decorations(false)` on Windows and Linux), so the word "Hypergate" is on screen once instead of twice and the mark sits in the actual top-left corner of the window. The version pill moved from beside the wordmark to the right group with the theme control and the health chip, and the minimise / maximise / close buttons continue that line in the app's own background. macOS keeps its traffic lights rather than growing a second set: a transparent title bar over a full-size content view, with `with_traffic_light_inset` putting them on the wordmark's line and the bar's leading padding starting after them. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **Everything the frame used to do, over IPC**: `window:minimize`, `window:maximize`, `window:drag` and `window:close` join the messages `window.rs` already understood, and `Wake::Window(WindowCommand)` carries them into the event loop where the window lives. **Drag is IPC, not `-webkit-app-region`**: support for the CSS property across WebView2, WKWebView and WebKitGTK is uneven, while `drag_window()` behaves the same on all three — the pointer is still down when the message lands, which is what continues the gesture. `WindowEvent::CloseRequested` now forwards to the same `WindowCommand::Close` arm the page's button raises, so Alt+F4, a Mac traffic light and our X are one code path and the close question cannot drift between them. The shell pushes `__hypergateOnWindowState` after every resize, because snapping and edge-dragging change whether the middle button should say "restore" without going through it. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **Double-click has to be answered on mousedown**: the first press hands the pointer to the OS move loop, which owns the mouse until the button comes up, so a `dblclick` listener on the bar never fires and double-clicking it did nothing at all. The second press *does* arrive, carrying `detail === 2`, and `onTitleBarMouseDown` sends `window:maximize` for it instead of another `window:drag`. One handler for the whole pointer contract, which is the only shape that works. Caught by the Devin review on PR #64. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **The same bundle still serves a browser tab**: `apps/web/src/lib/shell.ts` reads `window.__hypergateShell`, injected by the shell before the page loads, and its *absence* is the signal — no object, no window buttons, no full-bleed bar, because a tab already has chrome. That is also why `buttons` is a flag rather than inferred from the platform string: macOS is in the shell and still must not draw them. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **The footer is a rail, not a line under the column**: moved out of `.wrap`, so the rule reaches both window edges and the credit and the GitHub mark sit in opposite corners instead of a hand's width apart in the middle. Its contents go edge-to-edge only in the shell, where the top bar already does; in a browser they keep to the same 1360px column as the rest of the page, or the two rails would disagree on a wide window. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-06 · Done: 2026-08-06

## Epic 39: production hardening and agent-ready management (v1.0.2)

This is a deliberate patch release rather than the default minor bump: the requested 1.0.2 release packages security fixes, installer corrections and the completed agent-facing CLI.

- [x] **Local browser and DNS-rebinding attacks are blocked**: loopback Host validation, browser-origin checks on every management mutation, restricted management CORS and loud test-only auth bypass handling protect the local daemon without breaking Vite, MCP or native clients. · [spec](SPEC.md#32-gateway-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **Every update payload is integrity-checked**: npm payloads retain their existing verification and GitHub release tarballs now require matching SHA-256 manifest entries before staging. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **Agents can manage Hypergate without the UI**: `doctor`, global `--json`, scoped credentials and rotation, harness connection, CLI catalog installation and analytics are available through the shell. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **Native installers finish cleanly**: Windows process and PATH handling, Linux desktop refreshes and macOS uninstall behavior are safe and explicit. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-06 · Done: 2026-08-06
- [x] **The release records the deliberate patch bump**: `1.0.2` is used instead of the default minor version because this release is the requested production-readiness patch. · Added: 2026-08-06 · Done: 2026-08-06

## Epic 40: the Mac daemon starts, and startup can recover (v1.0.3)

This is another deliberate patch release: the requested 1.0.3 fixes the signed macOS daemon's hardened-runtime contract and makes the desktop shell's startup wait observable and recoverable instead of terminal.

- [x] **The signed Mac daemon can reserve V8's code range**: Node's SEA daemon receives `com.apple.security.cs.allow-jit` and `com.apple.security.cs.allow-unsigned-executable-memory` when the bundled and standalone binaries are signed under the hardened runtime; the Rust shell keeps its tighter signature, and CI inspects the daemon inside the shipped DMG before declaring the artifact runnable. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-08 · Done: 2026-08-08
- [x] **Starting Hypergate narrates itself and can recover**: the shell polls a token-free health endpoint with a 30-second initial budget, updates the existing splash text at milestones, records the last health error and elapsed time, and continues a slower background poll after the failure page so a daemon that becomes ready later still opens the manager. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-08 · Done: 2026-08-08

## Epic 41: Intel Mac artifacts actually launch (v1.0.4)

This is a focused patch release: the Intel download previously paired an x86_64 shell with an arm64 SEA daemon, so an Intel Mac could never start the gateway.

- [x] **Intel Mac releases contain an Intel daemon**: cross-architecture macOS builds inject the SEA blob into an official Node base for the target architecture, verify that archive against Node's `SHASUMS256.txt`, and assert the daemon's target Mach-O architecture alongside its V8 entitlements in the standalone and shipped-DMG smoke checks; native Apple Silicon builds keep their existing path. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-09 · Done: 2026-08-09

## Epic 44: merging a version bump cuts the release (v1.3.0)

Philip: *"let's also update that every PR for kotrain or hypergate cuts a new github release and installers when the version number is updated."* §4.1 says every PR bumps the minor, but releases were cut by hand-tagging, so the bumps outran the tags: v1.1.0 and v1.2.0 were both bumped and never released, and the last thing users could download was v1.0.4. This release closes that gap and ships both of them.

- [x] **`version-release.yml`**: watches pushes to `main` that touch `package.json` and, when that version has no tag yet, tags it and dispatches `release.yml`. It **tags and dispatches** rather than only tagging, because a tag pushed with `GITHUB_TOKEN` cannot trigger another workflow (GitHub blocks that to prevent recursion), so tagging alone would leave the release unfired while looking like it worked; `gh workflow run release.yml --ref <tag>` gives that run the same `github.ref_name` a hand-pushed tag would, so nothing inside `release.yml` needed changing. No PAT, so no secret to provision. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-08-13
- [x] **It refuses to tag a release that cannot build**: `node scripts/bump-version.mjs --check` (the repo's own seven-file assertion, which needs only Node built-ins, so no install step) and the existence of `docs/releases/<version>.md`, which `release.yml` reads for its notes. Both run **before** the tag exists, so a partial bump or missing notes fails while there is still nothing tagged, instead of leaving a tag pointing at a release that never built. A push that touches `package.json` without changing the version finds the tag already present and no-ops. Which makes the notes file part of the bump's own PR, now written into §4.1 and `docs/releases/README.md`. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-08-13
- [x] **Same workflow in kotrain**, adapted to that repo (root/desktop/server version agreement instead of the seven-file check, and it dispatches `docker.yml` too). Kotrain's `release.yml` gained the `workflow_dispatch` trigger hypergate's already had. · Done: 2026-08-13
- [x] **A `schedule` tick backs the `push` trigger up, learned the hard way over there.** Kotrain shipped this with `push: branches: [main]` alone and its v0.6.0 release silently did not happen: that repo auto-merges with `GITHUB_TOKEN`, and **a push made with that token never starts a workflow run** — the same recursion guard this workflow already routes around for the bot-pushed tag, one step earlier in the chain. Merges here are human, so the push trigger is the fast path and does fire, but a `*/15` tick now notices an untagged version bump whoever merged it, so adding auto-merge later can't quietly break releasing. Every path stops once the tag exists, and a `concurrency` group keeps a tick and a push from racing to create the same tag. · [spec](SPEC.md#39-distribution-shipped) · Done: 2026-08-13

## Epic 43: Kotrain connects itself (v1.2.0)

Philip: *"kotrain and hypergate should be a 1 click easy connection in either direction. Neither of them are connecting easily."* From this side, Kotrain was a `config` target: copy a snippet, find `~/.kotrain/settings.json`, paste it in. Worse than tedious, it was wrong. Kotrain holds its settings in memory while it runs and writes the whole file back on the next change, so a config written underneath a running app is overwritten by it. There was no file to write and no CLI to run, so the answer was to let the app do it.

- [x] **A fourth connect method, `deeplink`**: `ConnectMethod` gains it, `AgentConnectTarget` gains `deepLink`, and `connectDeepLink()` in `packages/core/src/connect.ts` builds `kotrain://hypergate/connect?port=N` from the gateway URL. The port is the entire payload: a token in a URL is a credential handed through the OS's link handler on the word of whoever opened it, so the client reads its own back over loopback (`POST /api/clients/resolve {key:'kotrain', create:true}`) and asks its user before acting. Pure and unit-tested, including that no other client claims a link and that a malformed gateway URL still points at 7777. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-13
- [x] **The panel is one button**: the Kotrain target's connect panel is **Connect Kotrain**, with the `~/.kotrain/settings.json` snippet still underneath for a machine that does not have the app yet, since a link needs something to open it. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-13
- [x] **The shell builds the URL, the page only names the client**: `open_external` in `apps/shell/src/window.rs` takes http(s) only, on purpose, because URLs reaching it can come from a page we did not write (a remote server's sign-in flow). So the page posts `connect:kotrain` and `client_deep_link()` builds the URL from `paths::port()`. Widening `is_web_url` would have been two characters and would have let any page in the webview launch anything. Two Rust tests cover it: the known client's link carries our port and no token, and an unknown name is a no-op. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-13
- [x] **Verified against an isolated daemon** (port 7801, own `HYPERGATE_DIR` and keychain namespace, never the real one on 7777) with a real Kotrain build: the connect API returns `method: "deeplink"` with the right port, the panel renders the button and the fallback snippet, and the link drives Kotrain end to end (running instance and cold launch), after which this manager shows Kotrain **Active** rather than the "Offline, never used" it had been stuck at. The browser path was clicked; the shell's IPC branch is covered by its unit tests. · [spec](SPEC.md#34-ui-shipped) · Done: 2026-08-13

## Epic 42: a broken remote endpoint says so in one line (v1.1.0)

Philip, on connecting the curated Vercel entry: *"connecting hypergate to vercel mcp server got this error"*, followed by a full Next.js 500 page pasted as the error. `https://mcp.vercel.com` was answering HTTP 500 to every request shape (unauthenticated, with a bearer, with a garbage body, on `GET`, and on a nonexistent team/project path that should have been a 404), while its two `.well-known` documents and its static assets still served normally. The endpoint was failing upstream, but nothing Hypergate showed said that.

- [x] **A remote server's error is a sentence, not the page it came back with**: a hosted endpoint that breaks answers with its host's HTML error page, and the SDK pastes that whole body into the message it throws, so `inst.error` became kilobytes of markup, rendered verbatim in the server row, in the expanded row, and in the toast at `App.tsx`, with the status code (the one part that says what to do) buried in it. `describeConnectError` keeps the status and drops the page: *"HTTP 500: the endpoint answered with an HTML error page instead of MCP. The host is failing upstream, so there is nothing to fix in this config."* The advice branches on the status, because 401/403 means sign in again, 5xx means it is not your config, and everything else means check the URL. Non-HTML messages are still passed through, capped at 500 characters so no other body dump can take the row's place either. The response itself is still what you debug with, so it goes to the log panel first, leaving `lastLog` as the readable line. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Added: 2026-08-13 · Done: 2026-08-13
- [x] **The status decides whether a server needs authorizing, not the presence of "401" in the text**: `is401` lowercased the whole error message and looked for `401` or `unauthorized` anywhere in it. An HTML error page carries plenty of incidental text, and Vercel's own 500 page ships a `_error-401cf280a3bb.js` chunk in a `<script src>`, which is enough to put a server that is simply down into `authorizing` and send the user to a pointless sign-in. `httpStatusOf` reads the real status off `StreamableHTTPError.code`, or out of the `(HTTP nnn)` the SSE transport spells into its message, and an exact `=== 401` decides it. The text heuristic stays as the fallback for errors that never had a status, which is what `UnauthorizedError` from the OAuth flow is. Only a number in 100–599 counts, since `code` is also where the SDK puts `-1` for an unexpected content type and where Node puts strings like `ECONNREFUSED`. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Added: 2026-08-13 · Done: 2026-08-13
- [x] **Verified against the failure itself, not a mock of it**: three tests in `supervisor.test.ts` drive a local server that answers with a host-shaped error page whose markup contains `401`: a 500 becomes `errored` with a 144-character message and no markup, that same page does not become an auth challenge, and a real 401 still becomes `authorizing`. Then the built `Supervisor` was pointed at the live `https://mcp.vercel.com` and returned exactly the intended line, with the page preserved in the logs. 190 core tests, 33 daemon tests, clean build. · [spec](SPEC.md#31-server-runtime--supervisor-shipped) · Added: 2026-08-13 · Done: 2026-08-13

## Epic 45: a Mac installer that opens, and explains itself (v1.5.0)

Philip, after trying the current DMG: it copied to Applications but macOS called the app damaged, and the installer window was oversized and visually empty. The desired install is a compact branded window with a fun drawn arrow and one plain instruction.

- [x] **Keep the shipped app's code-signing seal valid**: package every app resource before sealing the bundle, then run `codesign --verify --deep --strict` against `Hypergate.app` itself. The existing script signed the bundle while `web/` was absent and restored it afterward, which made every web asset an unsealed addition; the DMG's own notarized signature still passed, hiding the invalid inner app until Gatekeeper checked it after the drag. The release smoke now mounts the exact DMG and refuses that state before upload. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-17 · Done: 2026-08-17
- [x] **Turn the DMG into a compact installation card**: open at 660×400 content size (660×422 including the title bar) with the toolbar, sidebar, path bar and status bar hidden; place Hypergate and Applications at opposite ends of a deep-space blue-purple background; connect them with a hand-drawn cyan arrow; and keep the only instruction as `Drag it over into the Applications folder`. Finder persists the layout natively, release smoke reopens the finished image to assert it, and the generated background lives inside the sealed app so show-hidden-files users still see only two install targets. The volume is named `Hypergate <version>`, preventing an older mounted DMG from hijacking Finder's window while a new installer is built or opened. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-17 · Done: 2026-08-17
- [x] **Let Finder finish with the layout volume (v1.7.2)**: the Intel Mac build of v1.7.1 died on `hdiutil: couldn't unmount "disk9" - Resource busy` — Finder's `diskimages-help` still held the mount seconds after the window closed, which is a race, not a bug in the layout. `build-dmg.sh` now retries the detach for up to 30s and only forces as a last resort; forcing first would risk dropping the very window state the mount exists to persist. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-21 · Done: 2026-08-21

## Epic 46: the gate is made of light again (v1.6.0)

Philip: *"remove the grey line in the top part of the icon. for the loader and the icon in the app, can we give it a slow rotating glow that doesn't take much resources? would also love for the circle to have less defined lines and more like the website one."*

- [x] **The ring fades out instead of ending on a line**: `icon.rs` feathers both edges of the annulus with a supersample-scaled `smoothstep` band, and the generated SVG draws one gradient-filled circle through a radial-gradient alpha mask rather than stroked circles, so the mark reads like the site's shader at every size. The grey scratch across the top was a near-white hot arc composited over the crest in the raster, a `<path>` in the SVG, a white `::after` border in the manager CSS, and a stroked arc in both favicons — all four are gone, replaced by a wider ice bloom on the gradient crest plus an outer violet halo. `icon-192/512.png` were regenerated from the updated favicon, and tests assert the old `#d8fbff`/`#baf6ff` highlights never come back. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-17 · Done: 2026-08-17
- [x] **The Mac icon is the same mark, and stays that way (v1.7.1)**: `scripts/gen-icns.mjs` is a deliberate second copy of the mark's geometry, because the release job cross-compiles the Intel payload on an arm64 runner that cannot run the x86_64 binary to ask it for its own icon. It still drew the old stroked ring with the pale top arc, and its literal drift guard failed the macOS builds of v1.6.0 and v1.7.0 rather than shipping a stale icon — which is exactly what the guard is for. Ported the feathered annulus, the ice bloom and the blended halo across, re-pointed the guard at the new literals, and checked the JS output against the Rust renderer at 16/32/256px: identical but for one 1/255 rounding step. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-19 · Done: 2026-08-19
- [x] **The gate turns without costing anything**: the startup window's loader and the manager's 28px mark are conic gradients under a radial mask, rotating on `transform` alone — a 16s sweep, a 27s reverse bloom, and a 7s opacity breath on the startup halo. **Why not the site's shader**: the startup window is the first thing drawn on a machine that is still booting the daemon, and a WebGL context there would compete with it for exactly the seconds it matters; a composited rotation never repaints and never leaves the GPU. `prefers-reduced-motion` stops all of it, and a failed start renders the gate `still`, so a frozen daemon does not look like a loading one. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-17 · Done: 2026-08-17

## Epic 48: the drift guard gets an early tripwire (CI-only, no bump)

Found while shipping Epic 47: the v1.7.0 tag's Release run failed in both macOS build jobs, and so had v1.6.0's two days earlier — the last *published* release was still v1.5.0, and nobody had noticed because "Release on version bump" (which only tags and dispatches) kept reporting success while the dispatched Release workflow died unwatched. The renderer fix itself shipped as [PR #85](https://github.com/nekko-labs/hypergate/pull/85) (v1.7.1, Devin's, opened when v1.6.0 first failed); a concurrently built duplicate ([PR #87](https://github.com/nekko-labs/hypergate/pull/87)) was closed as superseded — the same collision shape as Epic 35's version-number race.

- [x] **Run `gen-icns.mjs` in CI's macOS shell job**: the script is a deliberate JS copy of `icon.rs` (the Intel payload is cross-compiled on an arm64 runner that cannot execute it) whose literal guard fails loudly on drift — but until now only the release workflow ever executed it, so Epic 46's icon rework broke every macOS release invisibly. With the guard on the PR path, the next `icon.rs` change fails its own PR. No version bump (the Epic 35 "unless" case: CI-only, nothing shipped changes). · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-21 · Done: 2026-08-21

## Epic 47: the credential vault (v1.7.0)

Philip: *"Add in a new feature to securely store the credentials needed for the CLI and MCP servers (access tokens, api keys, etc). The goal is that we can gate key access for agents and cli usage. if cli's need reauthentication, they can be done by requesting hypergate to do this instead of the user having to handle the manual prompts. hypergate should guide the user with the links or terminal command to get the necessary credentials. make it easy to roll secrets, delete them, etc."*

- [x] **Store keys in the keychain, hand back only metadata**: `CredentialVault` (daemon) keeps one keychain entry per credential (`cred:<id>`) with the OAuth grants' file fallback, and a valueless metadata index in `~/.hypergate/credentials.json`. No reveal endpoint exists; a value leaves through spawn injection, the gateway's `credential_env`, or `/api/credentials/resolve`, and nowhere else. `HYPERGATE_NO_KEYCHAIN=1` keeps test daemons out of the real keychain, which the extended `smoke:features` (11 new assertion groups) relies on. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Gate keys per agent, deny-by-default**: `AgentClient.credentials` (absent = none, the opposite default from servers because a credential *is* the key), flipped one at a time at `POST /api/clients/:id/credentials/:credId` with the same wildcard-pinning arithmetic servers use (`setCredentialAllowed` wraps `setServerAllowed` rather than re-deriving it). The gateway builds its new builtin tools per request with the caller's scope closed over: `hypergate__credentials_list` and `hypergate__credential_env`, recorded like routed calls so Analytics names who took which key; the `hypergate` server id is now reserved so nothing can shadow them. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Reauthenticate CLIs by asking Hypergate, not the user**: `hypergate run [--agent] [--with] -- <cmd>` resolves the caller's allowed credentials over `POST /api/credentials/resolve` and spawns the command with values in env only (never argv, never printed); `hypergate cred set fly` reads the value from stdin and knows the guide. Guides (8 services, vendor-true URLs and commands, env-var aliases like `GH_TOKEN`/`GITHUB_TOKEN`) are served at `/api/credentials/guides`, printed by `hypergate cred guide`, and drive the UI's add flow. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Servers reference keys instead of storing them**: `credentialRefs` (env var → credential id) resolves at spawn through the supervisor's injected `secretsFor`, merged into the config's secrets *before* the runtime shapes the spawn so Docker's `-e` flags get it too; the add flow vaults secret-looking `requires` values (`looksSecret` heuristic, in shared so the browser and daemon agree) and auto-references an existing credential whose env var matches. Rolling restarts every referencing server onto the new value; deleting prunes every grant and reference and says what it touched. Mutations carry the master-token + same-origin guards (the shutdown precedent). · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21

## Epic 37: finding the right tool, and knowing it is the right one (v0.22.0)

Philip: install the Playwright CLI as the use case; add CLI lookup like the MCP servers have, and work out the best official online source for what counts as an official/recommended CLI; put a note directly under every MCP or CLI search result confirming it is the recommended approach, or pointing at the official provider's instead; and the GitHub OAuth connection still does not show up correctly.

- [x] **The CLI section can find tools, not just recognise them**: `packages/core/src/cli-search.ts` looks a tool up in the **npm registry** and **Homebrew core**, `/api/clis/search` annotates each hit with whether the command is on this machine and what the verdict on it is, and the Command-line tools section shows install routes for the platform that asked. **Why those two sources**: there is no single official registry of CLIs, so the question is really "what is authoritative per distribution channel". npm is the channel Hypergate can always install from (Node is already a hard requirement) and the only one whose records state `bin` (which is what makes a package a CLI rather than a library), the publisher, the repository and the maintainer's own deprecation notice; Homebrew core is the largest curated, officially maintained catalog of native CLIs, built from each project's own release, and the only source here that publishes real install counts. Rejected on purpose: **winget** (its client talks to undocumented Store infrastructure; there is no documented public search API), Scoop and Chocolatey (community buckets, no first-party guarantee), asdf/mise (plugin lists, not tool catalogs), so Windows install commands are hand-written on curated entries instead. A Homebrew hit reports `installed: true` or nothing, never `false`: the API does not list a formula's executables, so `ripgrep` installing `rg` would otherwise be reported missing. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05
- [x] **The official Playwright CLI, and its MCP counterpart**: `@playwright/cli` (bin `playwright-cli`) is a curated, recommended entry, and `npx @playwright/mcp@latest` is a curated server. They are not interchangeable and each says so: the MCP server keeps a stateful session and streams page state into the model's context, while the CLI writes it to disk and lets the agent read what it needs. The server is the right answer for a harness that cannot run shell commands; the CLI is the cheaper one everywhere else. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05
- [x] **One sentence under every result saying whether it is the one to use**: `packages/core/src/advice.ts` turns each signal into a verdict plus, where the answer is "not this one", the provider's own route as an actionable button; the daemon attaches it to `/api/registry`, `/api/registry/search` and `/api/clis/search` so the browser never bundles core and the CLI can print the same line. Each signal claims only what it proves, which is the whole point: a curated flag was checked by a person against vendor docs; an **npm scope** is a publisher check the registry enforces (`@playwright/cli` can only be published by whoever owns `@playwright`), while an unscoped name proves nothing and needs its own hand-verified list (`playwright` is Microsoft's, and calling it a lookalike would be worse than saying nothing); a **deprecation notice** is quoted verbatim because it usually names its own successor. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05
- [x] **"Verified publisher" is not "Official"**: the MCP registry's namespace check proves control of a domain and nothing more, so `com.mcparmory/notion` used to carry the same ✓ Official chip as `com.notion/mcp`. The verdict now compares the namespace's organisation label against what the entry says it is, and a domain-verified third party gets **Verified publisher** with the distinction spelled out. The chip is derived from the same verdict as the sentence beneath it, so the two can no longer contradict each other. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05
- [x] **A `github.com` homepage is not a claim about GitHub**: matching the recommendation rules against an entry's homepage and registry id told *every* community server to go and use GitHub's official server instead, because they all live on GitHub and their ids all start `io-github-`. Matching now runs over the name and description with URLs stripped. Found by running the real registry, not by a test. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05
- [x] **GitHub browser sign-in works, because now there is an app to sign in with**. Verified live: `https://github.com/login/oauth`'s RFC 8414 metadata carries **no `registration_endpoint`**, so dynamic registration is impossible, and GitHub wants client authentication at the token endpoint even with PKCE. Hypergate ships no registered app, so Auto-connect could only ever fail: it errored with the names of two environment variables and rolled the server back, which is why the connection "did not show up". The catalog entry now declares the requirement, the UI asks the daemon *before* adding anything, and an unconfigured provider gets a three-step setup instead of an error: the provider's registration form, this daemon's exact redirect URI (the port is not always 7777), and the two fields. They go into the keychain entry beside that server's grant; resolution is config → env → keychain, so a packaged build that bakes in credentials still wins. An `authorizing` row with no app offers the same setup from its Sign in button. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05
- [x] **Two catalog entries that could never have worked**, found while building the feature. **Fetch** pointed at `@modelcontextprotocol/server-fetch`, which does not exist on npm at all (the reference fetch server is published to PyPI); it now runs `uvx mcp-server-fetch`. GitHub's **Run locally** used `@modelcontextprotocol/server-github`, marked "no longer supported" on npm, and now uses GitHub's own `ghcr.io/github/github-mcp-server` image. **Postgres** still runs, and its entry now says its reference package is unsupported upstream rather than implying otherwise. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-05 · Done: 2026-08-05

## Epic 36: grouped catalog connection options (v0.21.0)

- [x] **Grouped MCP connection options** — catalog entries can offer several connection methods for one logical server through a picker. GitHub is one entry with **Auto-connect** as the default OAuth option, **API key or token**, and **Run locally**; the standalone `github-pat` entry is removed. GitHub OAuth still needs a pre-registered client id, while the token option works without that setup. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-04 · Done: 2026-08-04

## Epic 35: the metadata catches up with the hero (v0.20.0)

Philip, after reviewing v0.18.0: do the two things the PR flagged as left undone.

- [x] **Every metadata surface now leads with the same promise as the hero**: `<title>` and both social titles become **Hypergate: every MCP server, one secure gate**; the meta description and the `og:`/`twitter:` descriptions name the agents and the services and state that keys and permissions stay out of an agent's hands; the JSON-LD `WebSite` description gives credential custody as the reason the product exists, and the `SoftwareApplication` description leads with it while keeping the mechanism detail and the `featureList` intact, because structured data is the one surface that *should* be exhaustive. `llms.txt` gained the same claim in its opening summary, since answer engines read that file and not the hero. The title's em dash went with the rewrite, per the house style. Lengths kept card-safe: 44-character titles, a 172-character meta description, 202-character social descriptions. · [spec](SPEC.md#37-marketing-site-shipped) · Added: 2026-08-04 · Done: 2026-08-04
- [x] **The subhead's comma splice**: "Hypergate makes it easy, provides a safety layer" → "makes it easy, **and** provides a safety layer". · Added: 2026-08-04 · Done: 2026-08-04
- [x] **The release notes moved with the version**: `docs/releases/0.19.0.md` became `0.20.0.md`, because v0.19.0 was bumped and never tagged and Epic 34's own rule says a release's notes cover everything since the last released tag. Its "everything since v0.17.0" line now names v0.18.0 *and* v0.19.0, and the hero bullet gained the sentence this epic earned: a search result and a shared link now say what the page says. Same handling v0.16.0 got. · Added: 2026-08-04 · Done: 2026-08-04
- [x] **The read-through Epic 34 prescribed, actually run, and it found three gaps**: `git log v0.17.0..HEAD --no-merges` against `docs/releases/0.20.0.md` showed the notes never covered the **UI refresh** (#55: the one-button add flow and its requires-only form, `cloud`/`local` runtime labels with official cloud servers sorted first, the analytics subnav plus the new Tool ranking and Security sections, and the hollow gate mark across favicon, topbar and tray) or the **fresh-install analytics fix** (#57), because both landed after the notes were written and neither PR extended them. The headline also still promised "a bundle" that an earlier commit had deliberately removed from the notes; it is now "plus a plugin and a clearer manager". This is the failure mode Epic 34 named: the workflow warns when a notes file is *missing* and cannot tell that one is incomplete, so the read-through is the only thing standing between a tag and a release that undersells itself. **No version bump on this one** (the "unless mentioned" case): the file is `0.20.0.md` and v0.20.0 is the tag being cut, so bumping would move the notes off the release they describe. · Added: 2026-08-04 · Done: 2026-08-04

## Epic 49: update the Mac app from the Mac app (v1.8.0)

Philip, from the installed v1.7.2 manager: *"let's make the hypergate updater work in app."* The **How to update** button was not a failed update. The installer channel was deliberately marked manual before Mac releases were signed and notarized, and that old guard had never caught up with the release pipeline.

- [x] **The app downloads the artifact that can actually replace it**: a macOS installer-channel check now goes straight to the GitHub release and selects the exact versioned DMG for this architecture, with the release's `SHA256SUMS` entry required before it can stage and rechecked by the detached updater immediately before mount. npm installs keep the existing two-tarball path. This split is necessary because installing those tarballs into `Hypergate.app` would modify a sealed bundle and make Gatekeeper reject it. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **The native shell replaces the whole signed bundle safely**: it copies itself to a temporary executable, quits the tray and daemon, mounts the staged DMG read-only, and verifies `codesign`, Gatekeeper notarization, `app.hypergate.tray`, the requested version, and the same Apple TeamIdentifier as the installed copy. It copies beside the installed app, verifies again, swaps by same-filesystem rename, checks the new CLI's version, restores the old app on failure, records the result, and relaunches. No admin prompt, package manager, Node runtime, or mutation inside the signed bundle. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **The existing update rail becomes the UI**: `canApply` is now true for signed Mac app installs, so the screenshot's **How to update** fallback becomes the already-designed **Download & install**, **Download only**, and **Skip** actions, with the existing progress, restart, reconnect, and one-time result states. Windows installers remain manual until signed, and Linux packages remain manual because they need root. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Verified against the real notarized release without touching the installed app**: focused core tests cover Mac planning and exact architecture asset selection; Rust tests cover app-path, DMG naming, and the second checksum check; a debug v1.8 updater mounted the real v1.7.2 arm64 release DMG and replaced a temporary `ditto` copy of `/Applications/Hypergate.app`. The result passed disk-image and deep app code-signature checks plus Gatekeeper as `Notarized Developer ID`, reported `hypergate 1.7.2`, recorded success, removed the staged payload, and left the actual `/Applications` copy untouched. The complete pass is 213 core tests, 45 daemon tests, 53 Rust unit tests, 12 Rust integration tests, clean build, fmt, clippy and version checks, plus the packaged npm update smoke. The current v1.7.2 app still needs one final manual drag to v1.8.0 because updater capability runs locally; v1.8.0 and later can update in app. · Added: 2026-08-21 · Done: 2026-08-21

## Epic 50: macOS means Apple silicon (v1.9.0)

Philip: *"let's remove intel mac support"*, then *"also, let's have the build named apple silicon for the macos one. not everyone knows arm64 is the same."* GitHub retired `macos-13`, its last Intel runner, so the Intel artifacts had been cross-compiled on an Apple silicon runner for months: no build job, notarisation step or smoke check could execute what it produced, and the disk image job it added is what failed v1.7.1's release while the Apple silicon one succeeded.

- [x] **`darwin-x64` leaves every build and release path**: out of the artifact matrix, the npm platform packages (six optional dependencies become five), the release's versioned assets, stable aliases and bare binaries. The cross-architecture Node base step goes with it — downloading an official Node archive and checking it against `SHASUMS256.txt` only ever existed to give an Intel SEA daemon a matching runtime, so the macOS job now injects the blob into the runner's own Node. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-22 · Done: 2026-08-22
- [x] **An Intel Mac is told, not handed something broken**: Rosetta translates x86_64 for Apple silicon and not the other way round, so an arm64 disk image cannot open on an Intel Mac at all. The site's architecture override — which existed because Safari conceals architecture — is gone, and an Intel visitor gets **Needs an Apple silicon Mac** with no download URL and no terminal command instead of the picker. An Intel install that checks for updates is offered no asset rather than a DMG that will not mount, in both the daemon's GitHub path and the native shell's asset naming. · [spec](SPEC.md#37-marketing-site-shipped) · Added: 2026-08-22 · Done: 2026-08-22
- [x] **The download is named the way its owner reads it**: the stable alias is now `hypergate-macos-apple-silicon.dmg` and the site's button says **Download for Mac (Apple silicon)**, because "arm64" is a chip name and "Apple silicon" is what About This Mac says. The `-arm64` alias stays published alongside it, and the *versioned* disk image keeps its `-arm64` filename on purpose: 1.8.x's in-app updater asks the release for that exact name, so renaming it would strand every install that can currently update itself. · [spec](SPEC.md#39-distribution-shipped) · Added: 2026-08-22 · Done: 2026-08-22
- [x] **Checked by building both sites and driving a Mac browser at each**: the base build and this one served side by side, with Chromium reporting macOS and each architecture in turn, gave `Download for macOS ⌄` → **Download for Mac (Apple silicon)** on Apple silicon and → **Needs an Apple silicon Mac** with no URL, no terminal command and an empty version chip on Intel. Full pass: 212 core tests, 47 daemon tests, 9 site tests, 50 Rust unit and 12 integration tests, clean `npm run build`, site build, `cargo fmt`, `clippy -D warnings` and `version:check`. · Added: 2026-08-22 · Done: 2026-08-22

## Epic 51: the vault, from an agent's side (v1.10.0)

Philip, using the shipped v1.7.0 vault: *"I want an agent to have the context to know that it can retrieve keys from hypergate and when it needs one, hypergate can list available keys, provide a clickable link to open hypergate for the user to confirm access approval"*, plus four smaller asks: separate **add your own** from the provider list, add search over providers, fix `Cmd+V` in the credential fields, and make a stored value viewable again behind OS permissions.

Four of the five were straightforward. The interesting one was the last, because it reverses something v1.7.0 wrote down as a rule.

- [x] **Agents are told the vault exists, and can ask for a key**: the gateway now returns MCP `instructions` at `initialize` (the one channel every harness reads) naming the vault and its three tools. `credentials_list` also names credentials the caller may *not* fetch, with `allowed: false` and a request URL but no value and no masked hint, because an agent cannot ask for a key whose existence is hidden from it; `Settings.agentsSeeAllCredentialNames` (default on) restores the old opacity. A new `hypergate__credential_request` files a request with a reason, and a refused `credential_env` files the same one automatically, so the refusal itself becomes actionable instead of a dead end. Requests dedupe per (agent, credential) into one row with an attempt count, live an hour, cap at 50, and are held in memory: a daemon restart drops them and the agent re-files, which buys no migration and no disk write on a path an agent controls the rate of. `POST /api/credential-requests/:id/approve` flips the *same* grant the per-agent switch does, so there is no second permission path to keep in sync. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **54 providers, searchable, with "add your own" out of the list**: the guides moved to `packages/core/src/credential-catalog.ts` (that file is data, `credentials.ts` stays logic) and grew from 8 to 54 under the same trust rule, with every create URL checked to resolve rather than recalled. AWS and Google Cloud are deliberately absent (a key pair and a file-path credential, which the one-value model cannot express) and so is Clerk (its dashboard root 404s anonymously, so there is no stable page to link). Search matches name, service, env var, alias and CLI name, so `fly`, `flyctl` and `FLY_API_TOKEN` all reach the Fly row; it lives in `@hypergate/shared` because the add panel ranks guides in the browser, which does not bundle core. Unsearched, the panel shows eight collapsed categories instead of 54 rows. **Add your own credential** became its own dashed block below the list: it had been the last row *of* the list with identical markup, which is exactly why it read as a fifty-first provider. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Cmd+V works again, and it was never a React problem**: the manager window is raw `tao` + `wry` with no `NSMenu` at all, and AppKit delivers the standard editing shortcuts through menu key equivalents, so the keystroke died before WKWebView saw it while right-click Paste (WebKit's own menu, built in the content process) worked fine. `apps/shell/src/menu.rs` installs a minimal macOS app menu (App / Edit / Window, all `muda` predefined items, so the selectors and key equivalents are the platform's). This fixes every input in the app, not just the credential fields where it was noticed. macOS only: the other two webviews handle Ctrl+V themselves and a menu bar would fight the custom title bar. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Reveal, and the invariant it reverses**: v1.7.0's spec said "there is no reveal endpoint; a value leaves the daemon through exactly three doors." There are now four. The rule that line was protecting was never *the owner may not read their own key* (they can read the keychain entry with `security` or Credential Manager) but *the API must not hand a value to anything that merely reached it*, and the refusal to show a stored value pushed users toward rolling a key at the provider just to see it, which invalidates whatever else used the old one and teaches people to keep a copy somewhere worse. `POST /api/credentials/:id/reveal` needs the master token, a same-origin request, **and** a live OS consent prompt, and is the only door that requires proof of a *human*. `hypergate authorize` owns that prompt in a separate process, so the daemon can only ask: Touch ID through `objc2-local-authentication` (policy `deviceOwnerAuthentication`, so it falls back to the login password on a Mac with no sensor), Windows Hello through `UserConsentVerifier`, polkit through `pkexec`, and an honest refusal where none of those exist rather than a weaker check. Every reveal and every refusal is recorded in Analytics. The UI shows the value once with copy and re-masks after 30 seconds. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-21 · Done: 2026-08-21
- [x] **The exit code that would have lied to every existing install**: `hypergate authorize` returns 0/1/3 for authorized/denied/unavailable, but an *older* `hypergate` binary returns clap's exit 2 for a subcommand it has never heard of, and the daemon and the shell are separately installed artifacts, so that is the normal state right after an update. Mapping 2 to `denied` told those users "you were refused" when the truth was "the thing that asks is out of date". Found because the dev daemon picked up `/usr/local/bin/hypergate` v1.7.1 from `PATH` (searched before the dev build) and the smoke test reported a confident, wrong refusal. `verdictFromExit` now treats every unrecognised code as `unavailable` with a message naming the fix, since not knowing is not the same as being told no. · Added: 2026-08-21 · Done: 2026-08-21
- [x] **Roll starts where the new key is minted**: the row's existing **Roll** action now opens the provider's `manageUrl` (falling back to its create URL) in the user's browser while expanding the paste form in Hypergate. The visual control does not change, but the two halves of the operation now happen from one click instead of requiring the separate **Get it** action first. Guides without a verified provider page keep the local form-only behavior rather than opening an unrelated URL. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-22 · Done: 2026-08-22
- [x] **The Mac status and About icons are present**: reproduced the installed v1.9.0 app logging a successful status-item registration while showing no hollow-gate item in the menu bar. The v1.10.0 branch's new `Menu::init_for_nsapp` initialization, run before the tray is built, makes the status item visible in the same-session A/B check. The About metadata now receives the procedural full-color gate too, rather than leaving its icon unset. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-22 · Done: 2026-08-22
- [x] **Verified**: 232 core tests (19 new: catalog integrity, the alias/env-var collision rules, https-and-first-party links, search ranking and regex-metacharacter queries), 69 daemon tests (24 new: the request store's dedupe, TTL, cap, eviction order and cleanup, plus the exit-code contract), 57 Rust unit and 12 integration tests, clean build, `fmt`, `clippy -D warnings` and version check. The feature smoke test grew the whole loop end to end: a refused fetch files a request, `credential_request` dedupes and keeps its reason, answering refuses no-token and cross-origin callers, deny grants nothing, approve grants exactly one credential, `initialize` carries the instructions, and reveal fails closed. Manually: the Touch ID sheet really appears from a spawned child process, the deep link opens and highlights its row, Approve in the UI grants access and the agent's next `credential_env` succeeds, and the reveal path returns the value and re-masks. Before/after screenshots in the PR. · Added: 2026-08-21 · Done: 2026-08-21

## Epic 52: minimizing the Mac app is no longer a trap (v1.11.0)

Philip: *"when minimizing, I don't see the main app menu bar icon. I also don't see the task bar icon either."* On macOS the shell ran as an accessory app always (`ActivationPolicy::Accessory` + `set_dock_visibility(false)`, set once at startup and never changed), so the manager window had no Dock tile, no Cmd-Tab entry, and no reachable menu bar; macOS gives an accessory app's *miniaturized* windows no Dock tile either. Minimizing (the yellow traffic light, or the Cmd+M item menu.rs added on purpose) therefore sent the window nowhere. Worse, every restore path was a silent no-op against a minimized window: `ManagerWindow::focus()` was `set_visible(true)` + `set_focus()`, `makeKeyAndOrderFront:` ignores a miniaturized window, and tao's `set_focus` explicitly bails out on one, so Open manager, the tray double-click, and the second-launch handoff all did nothing.

- [x] **Regular app while the window is open, accessory when it hides**: `mac_presence()` flips `set_activation_policy_at_runtime(Regular)` + `set_dock_visibility(true)` whenever the manager window is created or re-surfaced, and back to `Accessory` at every hide-to-tray site (now funneled through one `hide_manager()`). The startup `Accessory` remains the launch state, so a login-time `hypergate tray` with Start minimized still puts nothing in the Dock. A Dock-icon click (`Event::Reopen`, which only fires while the icon exists) reopens or restores the window. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **`focus()` un-minimizes first**: `set_minimized(false)` before `set_visible(true)` + `set_focus()`, so every "bring it back" path restores a minimized window on all three platforms rather than silently doing nothing. · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Verified**: 57 Rust unit + 12 integration tests, `cargo fmt`, `clippy -D warnings`. Live on the dev Mac against an isolated daemon: the v1.10.0 build reports `lsappinfo ApplicationType="UIElement"` with the manager window open (no Dock tile: the trap), the fixed build reports `"Foreground"`, and drops back to accessory on hide. Left unverified by hand: the actual minimize → Dock-click → restore gesture (scripting it needs accessibility permission the session doesn't have); the deminiaturize call is tao's documented `deminiaturize:` path. · Added: 2026-08-25 · Done: 2026-08-25

## Epic 53: the CLI section grows hands (v1.12.0)

Philip, with a screenshot of the Vercel CLI search result: *"when searching for a new cli, or for an existing installed cli, let's show the command to install it and let's run it for them, showing the logs and status in app. If installed, they can uninstall it, repair it, or reauthenticate using the corresponding commands. The user could alternatively see the terminal command and copy it. (give npm/pnpm/yarm/curl/brew/chocolatey/wget/etc options that are available based on OS, not just npm)"* and *"Make sure when agents use the hypergate mcp, they can list the cli's and request one to be installed. It should encourage agents to always go through hypergate to install and manage clis, mcp servers, and credentials."*

The section could already find and describe tools (v0.22.0); everything it found ended at a Copy button, and the only executable path was the shell's `hypergate cli install`, blocking, primary-route-only, with inherited stdio. This epic gives the daemon the hands and keeps the shell's safety grammar.

- [x] **One lifecycle grammar, ported not reinvented**: `packages/core/src/cli-actions.ts` mirrors the shell's `parse_curated_install` (same metacharacter rejections, same launcher allowlist, tested against the same cases) and derives per-manager lifecycle commands: the mechanical uninstall/repair inverse per manager, and pnpm/yarn/bun equivalents of any npm route, which is a fact about those managers rather than a vendor claim, so the trust rule holds. Script (`curl … | sh`, PowerShell) and download routes stay copy-only: executing them means executing an URL. `enrichCliInstalls` annotates every catalog and search result, so the UI, the API and the CLI read the same routes. Chocolatey stays out of curated routes for the same community-bucket reason as v0.22.0's search sources; the launcher allowlist still accepts a hand-curated `choco` row if one ever earns its place. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **The daemon runs jobs the way it runs updates**: `CliJobRunner` (apps/daemon/src/cli-jobs.ts) spawns shell-free with stdin closed (a tool that wants a TTY fails fast with its own message instead of hanging), line-buffers stdout+stderr into a capped snapshot the UI polls at the update cadence, enforces one job per tool, a 15-minute timeout, a kill route and a bounded history, and clears the detection memo on finish so the installed list flips. `POST /api/clis/jobs` derives the argv server-side from catalog data or a validated package name in a fixed template (request text never reaches a command line), and is master-token + same-origin like the updater; reads stay open like the rest of `/api/clis`. `/api/clis/managers` reports which package managers this machine actually has, so the Install buttons are real choices. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Reauthentication is curated, honest, and only sometimes runnable**: nine tools carry their vendor's own sign-in command (`gh auth login`, `flyctl auth login`, …). Only the browser-flow ones that complete with stdin closed are marked runnable (flyctl, wrangler, az); the rest say why they need a terminal and offer Copy, because a Run button that always dies at the first prompt is worse than none. A reauth job runs the tool's own resolved binary, never a package manager. · Added: 2026-08-25 · Done: 2026-08-25
- [x] **The UI grows the controls where the commands already were**: every runnable route in a search result or the catalog gets **Install** beside Copy (disabled with the reason when that manager isn't on this machine); an installed row's routes swap to **Repair** + armed **Uninstall**; an installed curated tool's **Manage** panel adds the sign-in line and a route picker that defaults to what the resolved path suggests (`/opt/homebrew/bin/gh` → brew). The job renders inline: status pill, the exact command, a live `LogConsole`, Stop while running. A finished job re-asks an open search, so the row that just installed flips to installed instead of re-offering. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Agents list and request, the user approves and sees the log**: `hypergate__clis_list` (installed tools + catalog search) and `hypergate__cli_install_request` (curated id or npm/brew package + reason) join the gateway builtins; the request queue is the credential-request shape reused (dedupe per agent+tool, TTL, cap, in-memory), the CLI tools nav carries the pending badge, and **Install & approve** starts the job with the log on screen while **Deny** runs nothing. A master caller is pointed at the manager instead of the queue, and `GATEWAY_INSTRUCTIONS` now opens by naming Hypergate as how MCP servers, CLIs and credentials are managed on this machine, with the CLI steps spelled out like the credential ones. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Verified**: 243 core tests (11 new: the grammar's parity cases with the Rust allowlist, lifecycle inverses per manager, npm-family derivation idempotence, catalog-wide integrity of every runnable route and auth hint) and 99 daemon tests (15 new: real spawns through the job runner covering success, failure, unspawnable, kill, line cap and one-per-tool, plus the request store's dedupe/TTL/cap/resolve). The feature smoke grew three blocks: managers + enriched routes, the job routes refusing no-token/unknown-action/unknown-tool/hostile-package with zero jobs started, and the agent loop end to end (list → request → dedupe with attempts → master told to use the manager → agent token refused on answer → deny resolves without installing), plus the instructions naming the CLI tools. Live on the dev Mac against an isolated daemon: a real `npm install -g cowsay@latest` from the search row with its log streaming in the row, the row flipping to installed with Repair/Uninstall, and a real uninstall leaving the machine clean; gh's Manage panel showing brew guessed from its path and the copy-only sign-in note; a real agent-filed flyctl request showing the badge and the approve/deny row. Before/after screenshots in the PR. · Added: 2026-08-25 · Done: 2026-08-25

## Epic 58: system package managers first, and scripts that run (v1.17.0)

Philip: *"for the cli installations, we should prioritize system package managers like brew, wget, etc."*

- [x] **The bias was in the ordering, not in the data**: `chooseInstall` took the *first runnable* route, and route order came from the catalog shape: the entry's primary `install` string, then platform extras, then the derived npm family. Tools whose primary was a URL (`node`, `git`, `uv`, `flyctl`) already got Homebrew first by accident; tools whose primary was an npm command (`claude`, `playwright-cli`, `wrangler`, `vercel`) got npm even on a Mac with brew. The web UI preferred an *installed* manager but scanned in the same order, so npm still won, and `sortCliCatalog` ranked the npm channel above Homebrew. Three places, one unstated preference. · [spec](SPEC.md#33-catalog--registry-shipped) · Added: 2026-08-26 · Done: 2026-08-26
- [x] **`rankInstall`: one tier table, applied once**: system-manager-present (0), vendor-script-present (1), language-manager-present (2), then the same three with the launcher missing (3 to 5), then not runnable (6). `enrichCliInstalls` now leaves `installs` ranked, the daemon answers `available` per route from one memoed PATH probe, and every surface reads that order instead of re-deriving one. The web UI lost `guessManager`'s manager-list juggling and the whole `/api/clis/managers` fetch with it, because the answer now rides on the catalog and the two can no longer disagree. An unprobed route counts as present, preserving v1.13.0's "an unknown answer never disables anything". · [spec](SPEC.md#33-catalog--registry-shipped) · Added: 2026-08-26 · Done: 2026-08-26
- [x] **Vendor scripts became runnable, behind three gates**: exact-match against `CURATED_SCRIPT_COMMANDS` (frozen at module load from the curated data), a narrow grammar for the two documented forms, and a five-host allowlist. The exact-match gate is the outer one on purpose: it is the only one that a cleverly-shaped string from a lookup or a request body cannot argue with. `parse_curated_script` mirrors all three in Rust, and the CLI additionally requires the daemon to have marked that exact command `runner: "script"`. `repair` re-runs the installer, which is how these vendors document upgrading in place; `uninstall` has no derived command and never reaches the shell. · [spec](SPEC.md#33-catalog--registry-shipped) · Added: 2026-08-26 · Done: 2026-08-26
- [x] **Vendor docs re-checked before adding a single route**: Claude Code documents four (native script, Homebrew cask, WinGet, npm) and they were all added; Cloudflare documents only the npm family for Wrangler, Vercel only npm/pnpm/yarn/bun, and Playwright only `npm install -g @playwright/cli`, so those three keep exactly the routes they had. The trust rule in `clis.ts` held: no community formula was invented to make a table look complete. · Added: 2026-08-26 · Done: 2026-08-26
- [x] **Two bugs the new tests found**: bun's curated PowerShell route was `irm bun.sh/install.ps1` with no scheme, which `irm` resolves over **http** before following a redirect. The host check refused it, and making the scheme explicit is strictly safer than the vendor's own copy. And `lifecycleFor` dropped `--cask`, so `brew install --cask claude-code` derived `brew uninstall claude-code`, naming a formula rather than the cask that was installed. · Added: 2026-08-26 · Done: 2026-08-26
- [x] **Verified end to end on the machine, not just in unit tests**: 317 core + 94 daemon tests (24 new), 61 Rust unit tests (4 new) plus 12 integration tests. Against a live daemon on an isolated port: `claude` resolves to `brew install --cask claude-code` (was npm) with `brew uninstall --cask claude-code` as its inverse; a real `curl … | bash` job ran through the shell and reported `bun was installed successfully`, exit 0; an agent-supplied package imitating a pipeline was rejected as an invalid package name; and `hypergate cli install claude` printed the Homebrew route. · Added: 2026-08-26 · Done: 2026-08-26

## Epic 57: the update that could never start (v1.16.0)

Philip, minutes after v1.15.0 released: *"it looks like the hypergate update is failing now"*.

- [x] **Diagnosed from the machine, not from the diff**: `/api/update` showed 1.15.0 already downloaded and staged with `canApply: true`, and `/api/update/progress` carried the real error — *"the installer did not start (Hypergate is still running)"*. `~/.hypergate/update.log` showed the update to **1.12.0 succeeding** and every 1.15.0 attempt failing identically after 30 seconds. Nothing to do with the v1.15.0 payload: the running app was 1.12.0, so the failing code was 1.12.0's updater, and the same code is still in `main`. · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Root cause: the updater's premise contradicts the daemon's design**: `apply()` asked the tray to quit on the comment *"a running tray owns the daemon, so asking it to quit takes both down together"*, and skipped stopping the daemon whenever that ask was delivered. But `daemon::spawn_detached` calls `setsid` **on purpose**, so managed servers survive the window closing — a daemon started before the current tray is not its child and outlives its exit. On the machine: the daemon holding `127.0.0.1:7777` had PPID 1 and had started four hours before the tray. `wait_for_quiet` requires both 7777 and 7778 free, never saw it, and aborted with the bundle untouched (correctly — it fails safe). Note `ask_running_instance_to_quit` could not have caught this: it returns whether the quit was *delivered*, not whether the daemon died. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Fix: confirm, don't assume**: deliver the quit, give the tray five seconds to take the daemon down, and stop it directly if the API port is still answering. When the tray really does own it the check clears immediately and nothing else happens. `port_busy` lost its `#[cfg(target_os = "macos")]` gate — it is pure std, and cross-platform `apply()` needs the same question answered. The ordering (`api::shutdown` before `daemon::stop`) matters more than it looks: see the stray-daemon note below. · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **A flaky test caught and made deterministic**: the new port test passed alone and failed in a full run — an ephemeral port claimed by something else between the drop and the assertion. The released-port half is now a bounded retry, with the reason written down, so the test is about `port_busy` rather than about who won a race. Three consecutive full runs green. · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Found while verifying the fix would work: `daemon.pid` can name the wrong daemon**. Two daemons were running from the same binary (one via `/Applications/...`, one via the `/usr/local/bin` symlink); the second had fallen back to port 7821 and overwritten `daemon.pid`, so the file named a daemon that was **not** the one holding 7777. `daemon::stop()` alone would have killed the wrong process. `api::shutdown()` runs first and goes to the API port, which is the right one, and its `!api::is_up()` guard then makes `daemon::stop()` a no-op — the existing ordering is what saves this, which is worth knowing before anyone reorders it. Not fixed here (own bug, own change). · Added: 2026-08-25
- [x] **Verified**: 58 shell unit tests (1 new) across three consecutive runs, 12 sandbox_exec integration tests, `cargo fmt --check` clean, `clippy -D warnings` clean, release build clean. The fix cannot be end-to-end verified against the stuck machine, because the updater that runs is the *installed* one — which is the point of the release-note caveat. · Added: 2026-08-25 · Done: 2026-08-25

## Epic 56: an agent can ask for a server (v1.15.0)

The other half of Epic 55's prompt: *"hypergate can configure any mcp server or cli and figure out what it needs and do it for the user. for credential entry, it just prompts the user's permission and sets it up"*. The resolver answered "what would this take"; this makes an agent able to ask, and the user able to say yes.

- [x] **The third leg of the request pattern**: `hypergate__server_resolve` (read-only) and `hypergate__server_install_request`, matching `cli_install_request` and `credential_request` in shape and in rule — an agent may look and ask, only the user approves, and approving is what has an effect. A `ServerInstallRequestStore` mirrors the CLI queue: deduped on (agent, server), TTL'd at an hour, capped at 50, oldest-first evicted, handing out copies. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Resolve before filing, and store the result, not the query**: an ambiguous or unknown name is thrown back at the agent (which can act on it) rather than queued for the user (who cannot). And the row keeps the resolved entry, because re-resolving at approval time would mean the user approves one thing and Hypergate adds whatever the registry says a few minutes later — a different version, or after an ambiguous name gains a match, a different server. A *retry* does refresh it: an agent re-asking may be doing so precisely because a newer release shipped. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **One tail for every add path**: `commitServer` now holds the persist → OAuth-or-start → status sequence that the Add dialog and an approved request both need. They differ entirely in what they trust and how they validate, and not at all in what they then do; extracting it is what stops an approved request from quietly skipping the OAuth kick-off the dialog performs. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **The queue is visible where the servers are**: rendered above the list *and* above the empty state, since "No servers yet" is exactly when an agent is most likely to be asking, and the old empty state gave no sign anything was waiting. The row shows the launch command rather than only a name and version (a name says what a server claims to be; the command says what will execute), plus who asked, their reason, attempts, and a "Still needs" line from the setup plan. The MCP servers nav item carries the pending count like Credentials and CLI tools. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **The dev proxy is overridable**: `HYPERGATE_DEV_API` / `HYPERGATE_DEV_PORT` on `vite.config.ts`. Port 7777 is where a real Hypergate runs, so capturing UI evidence safely means pointing the UI at a throwaway daemon without editing a tracked file or screenshotting someone's real servers and credentials. · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Verified**: 306 core + 92 daemon tests (8 new for the queue), clean builds. Live agent-to-approval on an isolated daemon: an agent resolved `com.microsoft/azure`, got 12 candidates for the bare name `azure` instead of a guess, filed a request, retried into the same row, and was refused 401 when it tried to approve its own request. In the browser, the row showed the command and outstanding steps, **Add & approve** added and started the server (ready, 61 Azure tools, 1/1 agents), and the badge cleared — no console errors, light and dark both checked. · Added: 2026-08-25 · Done: 2026-08-25

## Epic 55: point it at a name and it works out the rest (v1.15.0)

Philip: *"The goal is where hypergate can configure any mcp server or cli and figure out what it needs and do it for the user. for credential entry, it just prompts the user's permission and sets it up"* — carried over from a session with no access to this repo, where a standalone resolver prototype was validated against the live registry.

- [x] **Every multi-version server was pinned to its oldest release**: `searchRegistry` collapsed the registry's one-row-per-version response by keeping the first row per name, on a comment asserting the registry "orders newest first". It does not — rows arrive oldest-first and lexically sorted, so `0.2.2` outranks `0.2.14` — and measured against the live registry **44 of 44** multi-version servers resolved stale (`ai.aetherwealth/mcp` pinned at 0.2.0 with 0.2.14 published; `ac.inference.sh/mcp` at 1.0.0 with 2.0.1 published). Selection now runs through the semver comparator `update.ts` already had, preferring stable over prerelease. Found by running the real registry, not by reading the code. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Three quarters of the registry was refused for no reason**: 117 of 155 distinct servers publish no package at all, only a hosted `streamable-http` endpoint, and every one was mapped to `runnable: false` with "not locally runnable yet" — while Hypergate has shipped a `remote` runtime with http/sse transports and OAuth since v0.5.0. They now map to real remote entries. Runnable share of a broad live search: **24% → 100%**. The existing test asserting `runnable: false` encoded the gap rather than a requirement, and was rewritten. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **`resolveServer`: one name, one pinned server**: `com.microsoft/azure` → `npx -y @azure/mcp@2.0.2 server start`, which is Microsoft's own documented invocation, derived from registry metadata with no Azure-specific code. A fully-qualified name goes straight to `/v0/servers/{name}/versions` (authoritative, and the only way to see a full history — a search is capped at 50 rows and Azure has 34 releases, so a 20-row search never sees the newest stable at all). `?search=` is the fallback for inexact names and worth avoiding: measured **0.9s to 24s for the same query**, against under a second for the versions endpoint. Names match exactly with an ambiguity guard, because `?name=` does not reliably filter (asking for `com.microsoft/azure` returned 71 unrelated names) and `?search=` is loose enough that `azure` matches `com.azurecarbon/kodiak`. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **`?version=latest` is a prerelease**: the registry's own latest for `com.microsoft/azure` is `3.0.0-beta.37` while the newest stable is `2.0.2`. Stable is the default and `prerelease=1` opts back in. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **The setup plan, and why the auth half cannot come from the registry**: `planSetup` returns ordered steps — runtime CLI, credential, browser sign-in, and `ambient` (auth belonging to another tool's login) — each marked satisfied against this machine's PATH and vault. It exists because **the registry describes how to run a server and almost nothing about authenticating one**: `com.microsoft/azure` and `io.github.github/github-mcp-server` both declare **zero** `environmentVariables`, and neither works without credentials. Azure reads whatever `az login` wrote; GitHub needs a PAT that Hypergate's curated catalog already knows about and the registry does not mention. The curated half is matched on package identity only. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **A shared subcommand nearly leaked another server's credentials in**: the first identity matcher tokenised every non-flag argument, so Azure's `server start` and Fly's `mcp server` shared the token "server" and Azure's plan demanded a `FLY_API_TOKEN`. Only the first argument after a package runner names a package; a bare command is its own identity; generic runners never match. This is the same failure class as v0.22.0's "a `github.com` homepage is not a claim about GitHub", and it has its own regression test. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **`mcpb` binaries are platform-specific and hash-pinned**: per-OS/arch bundles carry a `fileSha256` and name their platform only in the filename, spelled however the release was cut. One is selected only when it matches this machine; an unmatched binary is not a fallback. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **`GET /api/registry/resolve?q=`**: serves the resolution plus the plan, reconciled against the CLI detector and the vault, with advice attached. Budgeted at 30s rather than the catalog search's 8s, because it is an explicit "set this one up" and the registry's search latency spikes past 20s. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **One semver comparator, not two**: the first cut of `resolve.ts` shipped its own, which collided with `update.ts`'s existing export at the package index. The duplicate was deleted and the existing one reused. · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Verified**: 301 core + 84 daemon tests (58 new), clean `tsc` build across the workspace. Live against the real registry, not fixtures: Azure resolves to `@azure/mcp@2.0.2` of 34 versions with a plan asking for the Azure CLI and `az login`; GitHub's server resolves to `ghcr.io/github/github-mcp-server:1.10.1` and asks for the PAT its own registry entry never mentions; `azure` returns 12 candidates instead of guessing; `prerelease=1` returns `3.0.0-beta.37`. All three paths re-verified through the daemon on an isolated port and data dir. · Added: 2026-08-25 · Done: 2026-08-25

## Epic 54: About stops crashing, and one Install button (v1.13.0)

Philip, the morning after v1.12.0: *"when clicking on about hypergate, it crashes. let's also add in the version number into the top up to date, update section"* and *"I clicked on install and it did nothing. Let's also just make a single install button that uses what is selected. make the package manager/installer choice a horizontal toggle."*

- [x] **The About crash was a use-after-free, proven then fixed**: the crash report was a bare SIGABRT, so a temporary hook fired the About menu action programmatically (no accessibility permission needed: `performActionForItemAtIndex` over raw `msg_send`). First run reproduced a panic inside muda's icon PNG encoder reading width 0; under MallocScribble with the hook firing on every status tick, a different garbage-read panic (`NSString -initWithBytes`) surfaced, which together said freed memory, not any one bad value. Root cause: `menu::install()` dropped its `Menu`/`Submenu` handles on return, muda's `Menu` has a Drop impl that detaches its bookkeeping, and every native NSMenuItem keeps a raw pointer into that data; the About item is the only one whose click dereferences it (the Edit items are pure AppKit selectors, which is why Cmd+V kept working). Fix: `std::mem::forget(menu)` after `init_for_nsapp()`, with a comment saying why the leak is the correct lifetime. 5 of 5 scribbled runs clean after, versus a panic in 1 of 3 before. · [spec](SPEC.md#310-credential-vault-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **"Install did nothing" could not have shown why**: manager availability was fetched exactly once, and any failure (or a daemon still waking) left it empty forever, silently disabling every Install button with only a tooltip. Managers now refresh with the CLI list, an unknown answer leaves buttons live (the daemon answers with a real error if a manager truly is missing), and a missing manager is stated in text under the command instead of a grayed button. · [spec](SPEC.md#33-registry--catalog-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **One horizontal toggle, one action button**: the per-route button wall became a `.seg` toggle of manager labels with a single Install (or Repair + armed Uninstall when installed) that runs the selected route; script routes say they are copy-only in a sentence. `.cli-installs` gained `align-items: flex-start` so the toggle hugs its content. · [spec](SPEC.md#34-ui-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **The Updates headline names the version**: "You're on the latest version, v1.13.0". · [spec](SPEC.md#38-desktop-shell--cli-shipped) · Added: 2026-08-25 · Done: 2026-08-25
- [x] **Verified**: 243 core + 84 daemon JS tests, 57 shell unit tests, `cargo fmt`, `clippy -D warnings`, clean builds, HTTP smoke, all re-run inside this branch's worktree after a stray working-directory mixup was caught and reverted. Live on an isolated daemon: the vercel row renders npm/pnpm/yarn/bun as a toggle with one Install; a real cowsay install ran from the new button (log streaming), the row flipped to Repair/armed Uninstall on the selected route, and a real uninstall left the machine clean; Fly CLI's `curl | sh` route shows the copy-only sentence; Settings shows the versioned headline. Before/after screenshots in the PR. · Added: 2026-08-25 · Done: 2026-08-25
