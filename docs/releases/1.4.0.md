# The website now shows the app you download

This release refreshes hypergate.app so its product tour matches the current Hypergate manager.

### Features

- **Every product screenshot is current.** The six feature views now show the left-navigation manager, scoped agents, managed servers, analytics, the catalog, tool permissions, and CLI detection.

### Performance improvements

- **Current screenshots stay light on every screen.** Desktop and mobile receive right-sized WebP images, with PNG fallbacks, so the refreshed tour remains fast to load.
