# Hypergate can find itself on an npm install

One bug, found by updating a real machine instead of a test layout, with three separate symptoms.

### Bug fixes

- **npm installs can update themselves.** Global installs no longer fail because the shell executable is not on `PATH`.
- **npm installs use the shell integrations again.** Server sign-ins, gateway tokens, and startup settings use their normal operating-system integrations.
