# Homebrew-first installs for Vercel and Wrangler

### Bug fixes

- **Vercel and Wrangler now use Homebrew when it is available.** On macOS and Linux, Hypergate prefers hand-checked homebrew-core formulas that build each project's own release, while noting that the vendors document only the npm route; Windows remains npm-only and the stale Playwright formula stays excluded.
