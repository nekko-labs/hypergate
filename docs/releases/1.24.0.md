# Search discovers native Homebrew routes

### Bug fixes

- **CLI search now discovers Homebrew routes generically.** For the first few curated or npm results on macOS and Linux, Hypergate derives candidate formula names from the command and publisher, verifies the formula's status and homepage, and adds a noted Homebrew route when it matches; Windows and static catalog browsing are unchanged.
