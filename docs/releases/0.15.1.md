# Updates that finish, and Intel Mac builds again

Both fixes are about a release reaching you: one in the update itself, one in the build that produces it.

### Bug fixes

- **Updates now confirm the new version is serving.** One-click updates no longer mistake a healthy restarted service for the old version.
- **Intel Mac builds run again.** Intel releases are cross-compiled on Apple silicon instead of waiting for retired build hardware.
